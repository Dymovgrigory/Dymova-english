# Журнал разработки — Сайт «Фоксинбург» (dymova-english.ru)

> Этот журнал ведется для бесшовной передачи работы между агентами. Содержит полную историю общения, решений, кодов и текущий статус.

---

## Общая информация о проекте

- **Сайт:** https://dymova-english.ru
- **Платформа:** Tilda (Business)
- **Проект ID:** 2053071
- **Кабинет:** https://tilda.ru/projects/?projectid=2053071
- **Настройки:** https://tilda.ru/projects/settings/?projectid=2053071
- **Аккаунт:** профиль «Вероника», логин: e-mail владельца (передается через секрет, НЕ публиковать)
- **Репозиторий для журнала:** https://github.com/Dymovgrigory/Dymova-english

---

## Хронология работы

### Сессия 28 (Devin) — Фикс YML-фида «Образование» + общий аудит + wiki проекта

**Дата:** 4 июля 2026. Ветка: `devin/1783175719-audit-wiki`. Задачи владельца: (1) починить YML-фиды (пришло письмо Вебмастера об отклонении), (2) сделать общий аудит и wiki для передачи проекта.

**1. YML-фиды («Услуги и предложения в поиске»).** Разобрал точные ошибки в Вебмастере.
- **Образование (`feed_education.xml`) — ИСПРАВЛЕНО и ВЫЛОЖЕНО.** Было заблокировано 2 из 4 предложений: в `param name="Продолжительность"` стояла единица `unit="занятие"`, а Яндекс допускает только час/день/месяц (иначе «Не указана единица измерения unit в param»). Фикс: чтение → `unit="час">24`, грамматика → `unit="час">30` (совпадает с суммой `hours` в `План`). Проверил по офиц. примеру Яндекса и рубрикатору (categoryId 20001/10099 валидны; `План` с `order/unit/hours` — легитимен). Задеплоил на `gh-pages`, сбросил кэш jsDelivr (`purge.jsdelivr.net`), подтвердил, что CDN отдаёт `unit="час"`. Перепроверка у Яндекса — **автоматическая** (ручной кнопки «Перепроверить» в UI нет; в меню фида только «Изменить регион»/«Удалить фид»).
- **Вакансии (`feed_vacancies.xml`) — ОСТАВЛЕНО КАК ЕСТЬ (решение владельца).** Заблокированы 3 из 3. Кроме техошибок (нет `price`/`currencyId`/`param name="Конверсия"`) есть непреодолимые стоп-факторы: формат требует **минимум 25 наборов-сетов** (у школы 3 вакансии), **зарплату в каждой вакансии** (на сайте у преподавателей «зависит от нагрузки», число только у администратора «от 45 000»), и **страницы без попапов** (у нас модалка заявки). Показ практически невозможен → чинить смысла нет. Владелец выбрал оставить фид как есть (будет висеть в ошибках).
- Оговорка по «Образованию»: формат рассчитан на онлайн-курсы; часть курсов школы офлайн, поэтому ручная модерация может ограничить показ даже после автопроверки.
- Канонические исходники фидов восстановлены в репо: `prototype/seo_schema/feed_education.xml`, `prototype/seo_schema/feed_vacancies.xml` (раньше в ветке отсутствовали).

**2. Общий аудит — `AUDIT_OBSHIY.md`.** Сводный аудит по 8 направлениям (тех.SEO, скорость/CWV, мобилка, SEO-контент, CRO, аналитика, локалка, бот/CRM) + топ-10 действий. Данные — из живого сайта, Вебмастера, `AUDIT_SPEED_MOBILE.md`, `YANDEX_BUSINESS_PLAN.md`.

**3. Wiki проекта — папка `wiki/`.** 9 файлов (README + 01–08): обзор, архитектура, SEO, аналитика, конверсия/формы, локалка, бот/CRM, доступы/инфра. Чистое место для передачи вместо разросшегося DEVLOG.

**4. /novosti — предложена стратегия (решение владельца ОТЛОЖЕНО).** Сейчас `/novosti` — витрина из 2–3 карточек, уводящая в VK (трафик и SEO-вес утекают). Предложение: развернуть логику (статьи на сайте по своим URL, соцсети — как анонсы), два потока контента (события/трасты + полезное для родителей под informational-спрос), 2–4 поста/мес, разметка `BlogPosting`+`BreadcrumbList`, CTA+перелинковка, дистрибуция VK/TG/MAX. Полный план — в `AUDIT_OBSHIY.md` (раздел 4). **Ждёт решения владельца:** оформить блог + первые 3 статьи / только контент-план / позже.

**Хвосты для следующего агента:**
- Проверить через ~сутки, что education-фид в Вебмастере снял блокировку 2 предложений.
- Дождаться данных GA4 (24–48 ч) и отметить 3 ключевых события (форма/звонок/мессенджер) ключевыми.
- От владельца: 3+ фото в Яндекс.Бизнес; тестовые заявки со всех форм.
- Решение по блогу `/novosti`.
- Спринт 2: скорость/CWV (главная LCP ~18 c), CLS на grammar/reading, микроразметка рейтинга/Event, новые посадочные (ОГЭ/ЕГЭ/репетитор/взрослые/немецкий/китайский).

### Сессия 27 (Devin) — Фикс: кнопка «Записаться на диагностику» + дизайн форм-модалки

**Дата:** 4 июля 2026. Ветка: `devin/…-fix-diagnostika-form`. Правки в блоках главной, задеплоено на Tilda (pageid 151210576) и опубликовано.

Владелец сообщил: в блоке онбординга кнопка «Записаться на диагностику» → «Оставить заявку на сайте» не открывает форму; сама форма-модалка выглядит некрасиво (серая кнопка, подписи без цвета).

**Причины и фиксы:**
1. **Онбординг (`tilda_onboarding.html`).** Обработчик дропдропа `ch.addEventListener('click', e=>e.stopPropagation())` глушил клик по `[data-fxb-zayavka]`, и он не доходил до document-level обработчика модалки в футере. Фикс: клики по `<a>` внутри дропдауна пропускаем (закрываем дропдаун и даём событию всплыть), `stopPropagation` только для не-ссылочных кликов.
2. **Модалка заявки (`tilda_footer.html`).** Модалка в скоупе `#fxb-page`, но CSS-переменные (`--purple*`, `--ink`, `--muted` и т.д.) объявлены только на `#fxb-footer`, а `.fxb-btn-main` в блоке не определён (есть только на подстраницах). Итог на главной: подписи без цвета, кнопка «Отправить заявку» — серая дефолтная. Фикс: добавлены переменные для `#fxb-page` и самостоятельный фиолетово-градиентный стиль сабмита (`.fxb-zsubmit`), не зависящий от `.fxb-btn-main`.

Пересобраны min-блоки (`tilda_onboarding_min.html`, `tilda_footer_min.html`), синхронизирован `main_combined_v7.html`. Деплой: `tilda_deploy_main_fix.py` (records rec2422576651 онбординг, rec2422572651 футер) → publish. Проверено на живом сайте: клик открывает модалку, сабмит — фиолетовый градиент.

### Сессия 26 (Devin) — Спринт 1: цели-конверсии в Метрике + починка GA4 + Яндекс.Бизнес

**Дата:** 4 июля 2026. Ветка: `devin/1783159230-sprint1-audit`. Все правки трекинга — в кабинете Tilda (2053071, вход vkrivobokova4@gmail.com), в репозитории — документация.

#### 1. Яндекс.Метрика 109945462 — ЦЕЛИ СОЗДАНЫ (авто-определение, код не требуется)
Созданы 4 авто-цели (тип «JavaScript-событие»/авто-детект, без правки кода счётчика):
- **578168283** — «Отправка формы» (все формы)
- **578168629** — «Клик по телефону» (все номера)
- **578168992** — «Переход в мессенджер» (Telegram/WhatsApp/MAX/Viber/Skype)
- **578169121** — «Переход в соцсеть» (VK/YouTube и др.)
Вебвизор + карта кликов ранее включены (Сессия 20). Счётчик на лайве присутствует (`mainMetrikaId='109945462'`).

#### 2. GA4 G-9XMYR6MJGL — БЫЛ СЛОМАН, ПОЧИНЕН
- **Проблема:** на сайте GA4 был подключён МЁРТВЫМ синтаксисом Universal Analytics — `analytics.js` + `ga('create','G-9XMYR6MJGL','auto'); ga('send','pageview')`. UA отключён Google в июле 2023, а ID вида `G-…` для `ga()` в принципе невалиден → GA4 НИКОГДА не получал данные («Данные с вашего сайта пока не получены»). Причина: в Сессии 20 ID GA4 был вставлен в legacy-поле Tilda «Google Analytics» (Настройки → Аналитика), которое генерирует код UA.
- **Починка (Tilda):** очищено поле «Google Analytics» (там был `G-9XMYR6MJGL`, дававший ga()-код). В общий HEAD (Настройки сайта → Вставка кода в HEAD) добавлен корректный GA4-loader `gtag.js` (`https://www.googletagmanager.com/gtag/js?id=G-9XMYR6MJGL` + `gtag('js'…); gtag('config','G-9XMYR6MJGL')`). Метрика (109945462) и GTM (GTM-NTM62R9) не тронуты.
- **События-конверсии (тот же HEAD, guard `window.__fxbConvInit`):** `generate_lead` (по `tildaform:aftersuccess`), `phone_click` (по кликам `a[href^="tel:"]`), `messenger_click` c `method` = max/whatsapp/telegram (по кликам на max.ru / wa.me·whatsapp / t.me·telegram).
- **Проверка на лайве:** в чистом браузере (без блокировщиков) страница шлёт валидный хит `https://www.google-analytics.com/g/collect?v=2&tid=G-9XMYR6MJGL…`; `typeof gtag === 'function'`, `dataLayer` заполнен; в опубликованном HTML `gtag/js?id=G-9XMYR6MJGL` есть, `ga('create'`/`analytics.js` больше нет, `ym(109945462)` и `GTM-NTM62R9` на месте, сниппет `__fxbConvInit` присутствует.
- **Нюанс проверки:** в браузере VM стоит расширение-блокировщик Google Analytics (стабит `analytics.js`), поэтому GA4 Realtime под ним показывал 0. Проверку делал чистым headless-браузером — хит уходит. Отчёты/Realtime у нового ресурса наполняются в течение 24–48 ч.

#### 3. GA4 «Ключевые события» — ОСТАЛОСЬ (после 24 ч)
GA4 позволяет отметить событие ключевым только после того, как оно впервые пришло (кнопки «создать по имени» нет; вкладка «Недавние события» пока пустая — «Отчёты о событиях появятся в течение 24 часов»). Действие: через ~24 ч (или после тестовой заявки) отметить звёздочкой `generate_lead`, `phone_click`, `messenger_click`. Дефолтные `purchase/qualify_lead/close_convert_lead` — плейсхолдеры, не мешают.

#### 4. YML-фиды (Вебмастер) — оба «Проверяется»
Фиды вакансий и образования — статус «Проверяется», ошибок нет. Мониторинг; действий не требуется, пока не отклонят.

#### 5. Яндекс.Бизнес «Фокси Фокс» (карточка 101959457653, ★4.2) — данные подтверждены
Режим работы, сайт, телефоны — корректны. До 100%/«Синей Галки» нужны фото (мин. 3) — за владельцем. Чек-лист заполнения и план работы с отзывами — в `YANDEX_BUSINESS_PLAN.md`.

#### ХВОСТЫ ДЛЯ СЛЕДУЮЩЕГО
- GA4: отметить 3 события ключевыми (через 24 ч), затем проверить в отчётах.
- Владелец: тестовые заявки со всех форм → подтвердить доставку (почты + Telegram) и что в Метрике/GA4 сработали цели/события.
- Владелец: 3+ фото в Яндекс.Бизнес; старт сбора отзывов по `YANDEX_BUSINESS_PLAN.md`.
- YML-фиды: дождаться финального статуса.

#### Ключевые значения (Сессия 26)
- Метрика **109945462** (цели: 578168283/578168629/578168992/578169121) · GA4 **G-9XMYR6MJGL** (gtag.js, события generate_lead/phone_click/messenger_click) · GTM-NTM62R9
- Яндекс: kidsfoxclub@yandex.ru · Google: dymovateacher@gmail.com · Tilda: vkrivobokova4@gmail.com (проект 2053071)
- Яндекс.Бизнес: карточка 101959457653 «Фокси Фокс» ★4.2

### Сессия 25 (Devin) — аудит скорости + мобильной вёрстки (Спринт 1)

**Дата:** 4 июля 2026. Ветка: `devin/1783159230-sprint1-audit`.

#### ЧТО СДЕЛАНО
- Прогнан Lighthouse 12.8.2 в mobile-профиле по всем 14 боевым страницам сайта.
- Собраны метрики по каждой странице: performance score, LCP, CLS, TBT, FCP, Speed Index, total byte weight; INP в этой версии Lighthouse не был доступен, поэтому помечен как `n/a`.
- Сохранены HTML/JSON-отчёты в `/home/ubuntu/lh/` и подготовлена сводка для документации.
- Отдельно выполнен аудит мобильной вёрстки в iPhone-эмуляции 390px по скриншотам из `/home/ubuntu/mobile_audit/`.

#### КЛЮЧЕВЫЕ ВЫВОДЫ ПО СКОРОСТИ
- Главная страница: score 38, LCP 18.6s, вес 3.7 MB — самый тяжёлый и медленный URL.
- `grammar`: CLS 1.11 — худший CLS в наборе.
- `reading`: CLS 0.68 — заметный layout shift в середине страницы.
- `podderzhivayushchie-online` и `standartnye-offline`: score 54, CLS 0.94 — сильные сдвиги в hero/программном блоке.
- `doshkolniki`: LCP 12.3s — медленный hero и тяжёлые внешние ресурсы.
- `kontakty`: LCP 7.5s — карта и ранние ресурсы тормозят первую отрисовку.

#### КЛЮЧЕВЫЕ ВЫВОДЫ ПО МОБИЛЬНОЙ ВЁРСТКЕ
- Горизонтального скролла нет ни на одной из 14 страниц.
- Критичных поломок раскладки не обнаружено: шапка, бургер, hero, карточки, формы и футер отображаются корректно.
- Декоративные элементы, выходящие за вьюпорт, — ожидаемое поведение из-за `overflow` контейнеров.
- Минорные замечания: пустой белый блок карты на `/kontakty`, фиолетовые пустоты на курсовых страницах, тяжёлые PNG без оптимизации.

#### ЧТО НУЖНО ПОМНИТЬ ДАЛЬШЕ
- Детали и рекомендации собраны в `AUDIT_SPEED_MOBILE.md`.
- Для целей в Метрике / GA4, статуса YML-фидов и Яндекс.Бизнес нужен вход в аккаунты `kidsfoxclub@yandex.ru` и `dymovateacher@gmail.com` — браузер потерял сессию, ожидается доступ от владельца.

### Сессия 19 — КОНВЕРТАЦИЯ КАСТОМНЫХ ФОРМ НА ПРИЁМ TILDA (Вариант A)

**Дата:** 3 июля 2026. Продолжение Сессии 18. Ветка: `devin/…-forms-to-tilda`.

#### ЧТО СДЕЛАНО (код):
Все 5 «мёртвых» форм переведены на приём Tilda (заявки → 6 сервисов: 2 почты kidsfoxclub@yandex.ru + dymovgrigory@gmail.com и Telegram 89996960441). Использован **Вариант A** — минимальные хуки на существующую кастомную разметку, submit обрабатывает `forms.js`.
1. **Hero-форма** `#fxbEnrollForm` (`prototype/tilda_cta_enrollment.html`): убран `onsubmit="return false;"`; добавлены `js-form-proccess` + `data-formactiontype="2"` + `data-inputbox=".t-input-group"` + `data-success-callback="fxbEnrollSuccess"`; 6 скрытых `formservices[]`; `tildaspec-formname`; поля переименованы в осмысленные `name` (`Name`, `Ребёнок`, `Phone`, `Формат`) с `data-tilda-req`/`data-tilda-rule`; блок «спасибо» + `js-errorbox-all`; success-callback прячет форму и показывает благодарность. (Главная уже грузит forms.js — доп. скрипт не нужен.)
2. **4 курсовые формы** `.fxb-zform` (`page_reading/grammar/preparation/letnyaya_akademiya.html` + генератор `build_course_pages.py`): удалён wa.me-обработчик; форма получила те же хуки + 6 `formservices[]` + `tildaspec-formname` + скрытое `Курс`; поля `Name`/`Phone` с валидацией; success-callback `fxbZSuccess` переключает на view «Заявка сформирована»; из альтернативных контактов убрана кнопка WhatsApp; **на каждую курсовую добавлен `<script src=".../tilda-forms-1.0.min.js" async>`** (курсовые страницы native-форм не имели, forms.js на них не грузился).

#### КАК ЭТО РАБОТАЕТ (проверено реверсом на живом сайте):
- forms.js читает `#allrecords[data-tilda-root-zone]` → endpoint `forms.tildaapi.com`, биндит формы `js-form-proccess` внутри записей `.r.t-rec` (курсовая форма в модалке в DOM с загрузки — биндится; подтверждено: forms.js дописывает `form-spec-comments`).
- На submit forms.js валидирует `js-tilda-rule`, затем POST на `/procces/`. **Прямой POST без forms.js → `{"needcaptcha":1}`** — поэтому курсовым обязательно нужен forms.js.
- Приём защищён **Yandex SmartCaptcha** (та же капча, что у существующих native-форм): при отправке показывается «Я не робот» → после прохождения заявка уходит во все 6 сервисов. Это штатное поведение, не баг.

#### ✅ ДЕПЛОЙ ВЫПОЛНЕН (сессия 19, 3 июля 2026)
- PR #62 (код форм) влит; PR #63 (`prototype/tilda_deploy_forms.py` + пересобранный `tilda_blocks_min/tilda_cta_enrollment_min.html`) влит.
- Залито через `prototype/tilda_deploy_forms.py` (CDP :29229): hero-min → pageid 151210576 / rec2422572641; контент 4 курсовых → определяемые из опубликованного HTML записи (reading rec2422858251, grammar rec2422858941, preparation rec2422860461, letnyaya rec2421958001). Все `-> OK`.
- Опубликовано `tilda_publish_pages.py 151210576 151292376 151292406 151292476 151229566` — все 5 ОК.
- Проверка живого HTML: hero и 4 курсовые несут `js-form-proccess` + 6 `formservices[]`; forms.js подключён на курсовых; `onsubmit="return false"` и wa.me-редирект из формы убраны (в подвале общий WhatsApp остался — это не форма).

#### ✅ ПРИЁМОЧНЫЙ ТЕСТ (частично, сессия 19)
- Реальный сабмит hero-формы на живой главной: прошёл валидацию и вызвал Yandex SmartCaptcha → форма привязана к Tilda `/procces/`.
- Реальный сабмит модальной формы курса на `/reading`: та же Yandex SmartCaptcha → курсовые формы привязаны.
- **Не подтверждено с VM:** финальная доставка лида — из дата-центра капча приходит пазлом («нажмите в таком порядке»), а не чекбоксом; пройти её надёжно нельзя.

#### ⏳ ОСТАЛОСЬ (только владелец, из своего браузера):
1. Отправить тестовую заявку с каждой формы (капча = простой чекбокс «Я не робот») и подтвердить приход на 2 почты (kidsfoxclub@yandex.ru, dymovgrigory@gmail.com) + Telegram (749445545) в течение 5 минут.

---

### Сессия 18 — НАСТРОЙКА ПРИЁМА ЗАЯВОК (ПЕРЕДАЧА НОВОМУ БОТУ) — НЕЗАВЕРШЕНО

**Дата:** 3 июля 2026. Ветка работы по формам — конфигурация в Tilda (не код).

#### ЗАДАЧА ВЛАДЕЛЬЦА (дословно):
«Настроить кому и куда будут приходить заявки: почта **kidsfoxclub@yandex.ru**, Telegram по номеру **89996960441**, почта **dymovgrigory@gmail.com**. Протестировать ВСЕ формы реальными заявками — чтобы владелец увидел информацию на почтах и в Telegram.» Старые почты (dymova.tut@gmail.com, Dymova.eng.club@gmail.com) — убрать. Курсовые формы: «Переключить полностью на email+Telegram (убрать WhatsApp)».

#### ✅ УЖЕ СДЕЛАНО:
1. **Добавлены и активированы 3 новых приёмника** в Tilda (Настройки → Формы):
   - Email **kidsfoxclub@yandex.ru** — подтверждён владельцем.
   - Email **dymovgrigory@gmail.com** — подтверждён владельцем.
   - **Telegram** app-id-**749445545** (API Key 749445545, Secret Key 34ecffd44d456d5b06d49f336471b202) — активирован, тел. 89996960441.
2. **Все 3 приёмника привязаны ко всем формам** через «Подключить сервис ко всем формам на сайте».
3. **Удалены старые email-приёмники** dymova.tut@gmail.com и Dymova.eng.club@gmail.com.
4. **Переопубликованы все живые страницы** (native-формы теперь шлют на новые приёмники). Команда:
   `python3 prototype/tilda_publish_pages.py <pageid...>` (CDP :29229, секрет TILDA_PASSWORD).

#### КЛЮЧЕВЫЕ ЗНАЧЕНИЯ (проверено на живом HTML после переопубликации):
- **6 хэшей сервисов (`formservices[]`)** на native-формах главной страницы:
  - `aa53a2a49933944f1fb9c3aaf4590622`, `30ef9df34728007db12c86a2e7cf58b6`, `b7967c40cba6f8680cc7a33c7d4a3bd1` (СТАРЫЕ — CRM/Sheets/webhook/старый telegram)
  - `406ce69659f99227298278ae7b0939c9`, `111d799a5db07bce252609cabd29b3e0`, `7c1b855d6fc4254f9b2a189ae11b7360` (**НОВЫЕ 3 = новый Telegram 749445545 + kidsfoxclub + dymovgrigory**)
- **formskey** (per-page, из `#allrecords data-tilda-formskey`): у главной 151210576 = `9b188d4c6f6f7968354f49a20b2be85f`. **ВНИМАНИЕ: у каждой страницы свой formskey — читать со страницы.**

#### ⏳ ОСТАЛОСЬ СДЕЛАТЬ (для нового бота):
1. **Hero-форма `#fxbEnrollForm`** на главной (файл `prototype/tilda_cta_enrollment.html`, блок T123 на pageid 151210576) — сейчас `onsubmit="return false;"`, БЕЗ обработчика → заявки теряются. Запитать на Tilda.
2. **Курсовые формы `.fxb-zform`** на `/reading` (151292376), `/grammar` (151292406), `/preparation` (151292476), `/letnyaya-akademiya` (151229566) — файлы `prototype/page_reading.html`, `page_grammar.html`, `page_preparation.html`, `page_letnyaya_akademiya.html`. Сейчас JS-submit открывает `wa.me/79939232309` (WhatsApp). Переключить на Tilda (email+Telegram).
3. **Опубликовать** изменённые страницы.
4. **Отправить реальные тестовые заявки** со всех форм, проверить приход на 2 почты + Telegram, показать владельцу пруфы.

#### КАК TILDA ПРИНИМАЕТ ЗАЯВКИ (реверс forms.js — `https://static.tildacdn.com/js/tilda-forms-1.0.min.js`):
- forms.js вешает submit на формы с классом **`js-form-proccess`** и атрибутом **`data-formactiontype="2"`**.
- POST на **`https://forms.tildacdn.com/procces/`**, `Content-Type: application/x-www-form-urlencoded`.
- Тело = `t_serializeArray(form)` — ВСЕ named inputs/select/textarea (кроме file/reset/submit/button), **включая скрытые `formservices[]`**. Сериализация читает СЫРОЙ атрибут `name` — НЕ зависит от `.t-input-group`/`data-field-name` (те нужны только для валидации/вывода ошибок).
- forms.js дополнительно добавляет скрытые поля: `tildaspec-cookie`, `tildaspec-referer`, `tildaspec-formid`(=id формы), `tildaspec-formskey`(=`#allrecords data-tilda-formskey`), `tildaspec-version-lib`, `tildaspec-pageid`, `tildaspec-projectid`, `tildaspec-lang`.

#### ДВА ВАРИАНТА КОНВЕРТАЦИИ КАСТОМНЫХ ФОРМ (рекомендация — Вариант A):
- **A) Минимальные хуки на существующую кастомную форму:** добавить форме класс `js-form-proccess` + `data-formactiontype="2"` + `data-inputbox=".t-input-group"`, вставить 6 скрытых `<input type="hidden" name="formservices[]" value="…" class="js-formaction-services">` (6 хэшей выше), добавить `<div class="js-successbox" style="display:none"></div>`. Поля формы оставить с осмысленными `name` (напр. `Name`, `Phone`, и доп. поля hero: `Ребёнок`, `Формат`). Т.к. страница — Tilda, forms.js уже подключён и сам обработает submit. **Убрать текущий `onsubmit="return false;"` и кастомный wa.me-обработчик.** Проверить, что forms.js биндится к форме внутри T123-блока (может требоваться, чтобы форма была в DOM при инициализации — она есть).
- **B) Свой fetch:** повторить POST на `/procces/` вручную со всеми полями + tildaspec-* (см. выше). Сложнее, но полный контроль.
- ВАЖНО: имена доп. полей hero (`Ребёнок`, `Формат`) станут подписями в письме/Telegram — назвать читабельно кириллицей или латиницей без пробелов.

#### ШАБЛОН РАБОЧЕЙ NATIVE-ФОРМЫ сохранён на VM: `/tmp/native_form_template.html` (полная разметка формы `form2421715981` с 6 хэшами, .js-successbox, .t-input-group, errorbox, submit). Живой HTML главной: `/tmp/live_main2.html`.

#### ПОЛНЫЙ СПИСОК СТРАНИЦ (24, projectid 2053071) — из кабинета:
- ЖИВЫЕ (новый дизайн): `/`=**151210576**(alias index-new), `/kontakty`=151228006, `/doshkolniki`=151228606, `/mladshie-shkolniki`=151228676, `/podrostki`=151228746, `/letnyaya-akademiya`=151229566, `/online-zanyatiya`=151229606, `/podderzhivayushchie-online`=151229676, `/standartnye-offline`=151229756, `/reading`=**151292376**, `/grammar`=**151292406**, `/preparation`=**151292476**, `/novosti`=151324806, `/vakansii`=151324866, `/lmsfoxinburg`=135877726, `/news`(легаси-лента, не трогать)=132317976.
- СНЯТЫ С ПУБЛИКАЦИИ (-old, НЕ переопубликовывать): index-old=32889798, reading-old=137726126, grammar-old=137739566, preparation-old=130390566, vacant-old=146080046.
- Системные: 11025377 (спасибо), 11026209 (политика), 11025828 (404).

#### ИНСТРУМЕНТЫ/ДОСТУП:
- Chrome CDP: `http://localhost:29229` (авторизованная сессия Tilda). Секрет **TILDA_PASSWORD** (org-scope). Логин vkrivobokova4@gmail.com.
- Публикация: `python3 prototype/tilda_publish_pages.py <pageid> [<pageid>...]`.
- Редактор страницы: `https://tilda.ru/page/?pageid=<id>&projectid=2053071&edit=y`.
- Загрузка HTML в T123-блок: см. `tilda_upload_copies.py` / `tilda_update_*.py` (POST `/page/submit/` comm=saverecord, onlythisfield=code).

---

### Сессия 1 (предыдущий агент — Opus 4.8 / Devin)

**Дата:** 18 июня 2026

#### 1. Получен доступ к Tilda
- Вход через https://tilda.ru/login/ (логин: vkrivobokova4@gmail.com)
- Профиль: «Вероника», тариф Tilda Business
- Проверены все 10 страниц проекта (pageid перечислены в PROMPT_FOXINBURG.md)

#### 2. Проанализированы интеграции форм
Подключены и активны:
- Google Sheets
- Webhook (Albato)
- BigBen CRM
- Tilda CRM (x2)
- Telegram
- Email: dymova.tut@gmail.com, Dymova.eng.club@gmail.com
- Личный кабинет

> **Важно:** Любая новая форма Tilda автоматически шлет заявки во все эти сервисы. Настройки НЕ менять.

#### 3. Составлен PROMPT_FOXINBURG.md
Подробный промт на 176 строк с 16 пунктами задач, глобальными правилами и чек-листом. Запушен в ветку `devin/1782567621-foxinburg-prompt` репозитория Dymova-english.

#### 4. Получены данные от владельца
- **Адреса филиалов:**
  - Филиал 1: Долгопрудный, пр-кт Лихачевский, 76к1, тел. 8 993 923-23-09
  - Филиал 2: Долгопрудный, пр-кт Ракетостроителей, 9к3, тел. 8 916 732-31-69
- **Бейдж «Хорошее место 2026»:** `<iframe src="https://yandex.ru/sprav/widget/rating-badge/162408588499?type=alt" width="150" height="50" frameborder="0"></iframe>`
- **Немецкий язык:** педагог Саляхова Алина, группа 9000 руб/мес (2 раза/нед по 60 мин), индивидуально 2500 руб/час, пробное 1125 руб
- **Китайский язык:** педагог Шевченко Дарья, стоимость аналогичная
- Фотографии и видео — будут добавлены владельцем позже

#### 5. Создана рабочая копия главной
- Оригинальная главная (pageid: 32889798) НЕ тронута
- Копия: pageid **151210576**, URL: `https://dymova-english.ru/page151210576.html`

#### 6. Утвержден дизайн-подход
Владелец отклонил стандартные шаблоны Tilda. Утвержден подход:
- Все блоки — **кастомный HTML/CSS/JS** (вставляются через блоки «HTML-код»)
- Современный дизайн с аккуратными анимациями (scroll-reveal, hover-эффекты)
- Фирменные цвета: фиолетовый (#392852, #662d92, #6237a2), оранжевый (#ee7349), желтый (#fcf951)
- Шрифт: Montserrat
- Редактируемой остается только «начинка» (тексты, фото, видео)

Создан прототип секции «Как начинается обучение» — **утвержден владельцем** ("отлично! мне очень нравится!!!").

#### 7. Реализованные блоки на рабочей копии

| # | Блок | Пункт | Статус | Файл прототипа |
|---|---|---|---|---|
| 1 | Слоган «Английский не для школы, а для жизни!» | п.10 | ГОТОВО | (встроен напрямую) |
| 2 | Верхняя панель (бейдж + адреса + телефоны + Max Bot) | п.2,3,16 | ГОТОВО | tilda_topbar.html |
| 3 | Узкая шапка «Летние программы» | п.14 | ГОТОВО | tilda_summerbar.html |
| 4 | Блок «Как начинается обучение» (4 карточки + CTA) | п.9 | ГОТОВО | tilda_onboarding.html |
| 5 | Блок «Наши преимущества» (4 карточки) | п.11 | ГОТОВО | tilda_advantages.html |
| 6 | Блок «Наши направления» (3 карточки) | п.7 | ГОТОВО | tilda_directions.html |
| 7 | Блок «Команда» (карточки-заготовки) | п.8 | ГОТОВО | tilda_team.html |
| 8 | Заголовок фотобанка | п.5 | ЧАСТИЧНО | tilda_photobank_head.html |

#### 8. Где остановилась работа
Агент начал подключать **нативную форму Tilda** (popup lead form) для кнопки «Записаться на бесплатную диагностику» (п.12). Был в процессе поиска подходящего попапа формы в библиотеке блоков Tilda. **Сессия завершилась** из-за превышения лимита использования.

---

### Сессия 2 (текущий агент — Devin)

**Дата:** 18 июня 2026

#### 1. Восстановление контекста
- Репозиторий hVxR133/devin-hvxr133-06181749-05193707-5 содержал только README.md
- Прототипы из предыдущей сессии отсутствовали на диске (другая VM)
- Владелец прислал полную историю переписки и все HTML-файлы прототипов

#### 2. Восстановлены все файлы
Все 8 прототипов восстановлены в `/home/ubuntu/prototype/` и скопированы в репозиторий:
- foxinburg-onboarding.html (standalone прототип)
- tilda_advantages.html
- tilda_directions.html
- tilda_onboarding.html
- tilda_photobank_head.html
- tilda_summerbar.html
- tilda_team.html
- tilda_topbar.html

#### 3. Создана документация
- `PROMPT_FOXINBURG.md` — полный промт для агента с обновленной информацией
- `DEVLOG.md` — данный журнал разработки

### Сессия 3 (текущий агент — Devin, продолжение)

**Дата:** 18 июня 2026

#### 1. Перенос в репозиторий Dymova-english
- Репозиторий hVxR133/... был недоступен для push (ошибка 403 через git proxy)
- Все файлы перенесены в https://github.com/Dymovgrigory/Dymova-english
- Создан PR #1 с журналом, промтом и 8 прототипами: https://github.com/Dymovgrigory/Dymova-english/pull/1

#### 2. Построены новые блоки (6 штук)

| # | Блок | Пункт | Статус | Файл прототипа |
|---|---|---|---|---|
| 9 | CTA «Записаться на бесплатную диагностику» с формой-опросом | п.12 | ГОТОВО | tilda_cta_diagnostika.html |
| 10 | Фотобанк — галерея с лайтбоксом (заглушки для фото) | п.5 | ГОТОВО | tilda_photobank_gallery.html |
| 11 | Сворачиваемый блок «Сведения для образовательной организации» | п.6 | ГОТОВО | tilda_svedeniya.html |
| 12 | Карточки немецкого/китайского языков с попапами (записаться / пробное / звонок) | п.13 | ГОТОВО | tilda_languages.html |
| 13 | Выпадающее меню с подразделами (курсы + направления) | п.4 | ГОТОВО | tilda_dropdown_menu.html |
| 14 | Слоган «Английский не для школы, а для жизни!» (отдельный блок) | п.10 | ГОТОВО | tilda_slogan.html |

Также создана страница:
| # | Страница | Пункт | Статус | Файл |
|---|---|---|---|---|
| 15 | «Как с нами связаться» — контакты, карта, соцсети, часы | п.4 | ГОТОВО | page_kontakty.html |

#### 3. Особенности реализации CTA-кнопки (п.12)
- Кнопка «Записаться на диагностику» открывает выбор: (а) заявка на сайте или (б) написать в Max
- Заявка на сайте → модальное окно с формой: имя родителя, имя ребенка, возраст, телефон
- Скрытое поле `source = "Главная — Записаться на бесплатную диагностику (верхний блок)"`
- После отправки — экран подтверждения с автозакрытием через 3 сек
- В Tilda: привязать к нативной форме проекта для корректной доставки заявок

#### 4. Особенности карточек языков (п.13)
- Кнопка «Записаться на курс» → 3 варианта: заявка / Max / звонок
- Кнопка «Пробный урок» → попап с ценой (1125 руб), длительностью (60 мин), кнопкой «Оплатить»
- Указаны утренние группы для 2-й смены (п.15)
- Педагоги: Саляхова Алина (немецкий), Шевченко Дарья (китайский)

#### 5. Построены страницы направлений (3 штуки)

| # | Страница | Пункт | Статус | Файл |
|---|---|---|---|---|
| 16 | Для дошкольников (3-6 лет) — /doshkolniki | п.7 | ГОТОВО | page_doshkolniki.html |
| 17 | Для младших школьников (7-10 лет) — /mladshie-shkolniki | п.7 | ГОТОВО | page_mladshie_shkolniki.html |
| 18 | Для подростков (11-16 лет) — /podrostki | п.7 | ГОТОВО | page_podrostki.html |

Каждая страница включает: hero-баннер, блок «Как устроены занятия» (4 карточки), раздел «Пособия и материалы», CTA-блок с 2 кнопками (заявка/Max). Упомянуты утренние группы (п.15).

#### 6. Построены страницы летних программ (4 штуки)

| # | Страница | Пункт | Статус | Файл |
|---|---|---|---|---|
| 19 | Летняя Академия | п.14 | ГОТОВО | page_letnyaya_akademiya.html |
| 20 | Онлайн занятия | п.14 | ГОТОВО | page_online_zanyatiya.html |
| 21 | Поддерживающие онлайн занятия | п.14 | ГОТОВО | page_podderzhivayushchie_online.html |
| 22 | Стандартные оффлайн занятия | п.14 | ГОТОВО | page_standartnye_offline.html |

### Сессия 4 (агент — Devin, продолжение)

**Дата:** 18 июня 2026

#### 1. Вход в Tilda и создание страниц

Залогинились в Tilda (vkrivobokova4@gmail.com), проект 2053071. Создали 8 новых пустых страниц и 1 рабочую копию главной (из предыдущей сессии). Всего **19 страниц** в проекте (10 оригинальных + 9 новых).

#### 2. Создано 8 новых страниц с HTML-контентом

Для каждой страницы: добавлен блок T123 (HTML-код), вставлен HTML через Ace editor (JavaScript: `editor.setValue(html, -1)`), сохранено, настроены заголовок и slug.

| # | Страница | pageid | slug | HTML-файл | Статус |
|---|---|---|---|---|---|
| 1 | Как с нами связаться | 151228006 | /kontakty | page_kontakty.html (10911 chars) | ГОТОВО |
| 2 | Для дошкольников (3-6 лет) | 151228606 | /doshkolniki | page_doshkolniki.html | ГОТОВО |
| 3 | Для младших школьников (7-10 лет) | 151228676 | /mladshie-shkolniki | page_mladshie_shkolniki.html | ГОТОВО |
| 4 | Для подростков (11-16 лет) | 151228746 | /podrostki | page_podrostki.html | ГОТОВО |
| 5 | Летняя Академия | 151229566 | /letnyaya-akademiya | page_letnyaya_akademiya.html | ГОТОВО |
| 6 | Онлайн занятия | 151229606 | /online-zanyatiya | page_online_zanyatiya.html | ГОТОВО |
| 7 | Поддерживающие онлайн занятия | 151229676 | /podderzhivayushchie-online | page_podderzhivayushchie_online.html | ГОТОВО |
| 8 | Стандартные оффлайн занятия | 151229756 | /standartnye-offline | page_standartnye_offline.html | ГОТОВО |

Все 8 страниц — **не опубликованы** (только сохранены в редакторе).

#### 3. Рабочая копия главной — combined блоки

- **pageid:** 151210576
- **Заголовок:** «Главная (рабочая копия)»
- **Slug:** /index-new
- **URL:** https://dymova-english.ru/index-new

13 блоков главной объединены в **один HTML-файл** `main_combined.html` (92,459 chars, 1,329 lines) и вставлены в единственный блок T123 на странице.

Порядок блоков в combined файле:
1. `tilda_topbar.html` — верхняя панель (бейдж, адреса, телефоны, Max Bot)
2. `tilda_slogan.html` — слоган «Английский не для школы, а для жизни!»
3. `tilda_summerbar.html` — узкая шапка «Летние программы»
4. `tilda_cta_diagnostika.html` — CTA «Записаться на диагностику» с формой
5. `tilda_onboarding.html` — «Как начинается обучение» (4 карточки)
6. `tilda_photobank_head.html` — заголовок фотобанка
7. `tilda_photobank_gallery.html` — галерея с лайтбоксом
8. `tilda_directions.html` — «Наши направления» (3 карточки)
9. `tilda_team.html` — «Команда» (карточки с видеокнопками)
10. `tilda_languages.html` — карточки немецкого/китайского
11. `tilda_advantages.html` — «Наши преимущества» (4 карточки)
12. `tilda_svedeniya.html` — аккордеон «Сведения для образовательной организации»
13. `tilda_dropdown_menu.html` — выпадающее меню с подразделами

**Технический подход:** Вместо вставки 13 отдельных блоков T123 (что вызывало таймауты в UI Tilda), все блоки были объединены в один HTML-файл и вставлены через единственный блок T123. JavaScript через Ace editor (`editor.setValue(htmlContent, -1)`) — подтверждено: «OK, lines: 1329».

#### 4. Настройки страницы главной копии

Через Playwright + Tilda API:
- Заголовок: **«Главная (рабочая копия)»** (установлен через `input[name='title']`)
- Описание: **«Языковая школа Фоксинбург — рабочая копия главной страницы»** (через `input[name='descr']`)
- URL slug: **index-new** (через `input[name='alias']`)
- Сохранено через кнопку «Сохранить изменения»

#### 5. Верификация

Проверено на странице проекта (`tilda.ru/projects/?projectid=2053071`):
- Все 19 страниц отображаются в списке
- Главная копия показывает заголовок «Главная (рабочая копия)» и slug /index-new
- 8 новых страниц показывают корректные заголовки и slugs
- Оригинальные 10 страниц не тронуты

### Сессия 5 — Критические исправления по отзыву владельца (2026-06-18)

**Проблемы, выявленные владельцем:**
1. Верхняя шапка — нет раскрывающихся кнопок по категориям, нет ссылки на Max канал
2. Шапка «Летние программы» — слишком узкая, бейдж «Хорошее место» обрезан
3. Слоган — стандартный блок Tilda вместо кастомного в фирменном стиле
4. Ряд задач не выполнены — требуется сверка с чек-листом

**Исправления:**

#### 1. Выпадающее меню (`tilda_dropdown_menu.html`) — полностью переписано
- Sticky header с z-index 1000
- Логотип бренда с эмодзи лисы
- Две раскрывающиеся категории: «Наши курсы» (чтение, грамматика, подготовка к школе) и «Наши направления» (дошкольники, младшие школьники, подростки)
- Ссылка на Max канал (https://max.ru/id611904726658_biz) — кнопка справа
- Кнопка «Вход в ЛК» (→ /lmsfoxinburg)
- JavaScript: скрывает нативные Tilda headers (`.t228`, `.t188`, `.t210`, `.t-menu`)
- Мобильный бургер-меню (responsive)

#### 2. Верхняя панель (`tilda_topbar.html`) — исправлена
- Увеличен padding: `16px 28px` (было `10px`)
- Исправлен контейнер бейджа: `padding:6px 10px`, `overflow:visible`, `border-radius:12px`
- Добавлен разделитель `fxb-sep` между филиалами
- Добавлен блок часов работы: «Работаем ежедневно с 9:00 до 21:00»

#### 3. Узкая шапка «Летние программы» (`tilda_summerbar.html`) — исправлена
- URL-ы исправлены: `/online-zanyatiya` (было `/online`), `/podderzhivayushchie-online` (было `/online-support`), `/standartnye-offline` (было `/offline`)
- Увеличен padding: `12px 24px` (было `9px`)

#### 4. Слоган (`tilda_slogan.html`) — обновлен
- Кастомный градиент (#392852 → #662d92 → #6237a2)
- Шрифт Montserrat 800, жёлтая подсветка слова «жизни!»
- JavaScript: скрывает нативный Tilda текстовый блок с этим же текстом

#### 5. Новый порядок блоков (main_combined_v2.html)
1. `tilda_dropdown_menu.html` — выпадающее меню (ПЕРВЫЙ, чтобы скрыть нативную шапку)
2. `tilda_topbar.html` — верхняя панель
3. `tilda_summerbar.html` — узкая шапка «Летние программы»
4. `tilda_slogan.html` — слоган
5-13. Остальные блоки без изменений

#### 6. Очистка T123 блоков в Tilda
- Удалены 12 старых/пустых T123 блоков (оставшихся от предыдущих сессий)
- Остался 1 блок T123 с правильным combined HTML (96131 символов, 1356 строк)

---

## Оставшиеся задачи (TODO)

### ГОТОВО:
- [x] Все 22 прототипа HTML созданы и запушены в GitHub (PR #1, #2 — merged)
- [x] 8 новых страниц созданы в Tilda с HTML-контентом
- [x] Рабочая копия главной создана с combined блоком (13 блоков → 1 HTML)
- [x] Настройки страниц (заголовки, slugs) установлены
- [x] **Выпадающее меню** — полностью переделано (Наши курсы, Наши направления, Max канал, Вход в ЛК)
- [x] **Верхняя панель** — увеличена высота, исправлено обрезание бейджа, добавлены часы работы
- [x] **Летние программы** — исправлены все URL-ы (/online-zanyatiya, /podderzhivayushchie-online, etc.)
- [x] **Слоган** — кастомный стилизованный блок с градиентом и JS-скрытием нативного блока
- [x] **Очистка T123 блоков** — удалены 12 старых/пустых блоков, остался 1 (новый combined)

### Осталось сделать:
- [ ] **Подписи источника** на всех формах проекта (скрытое поле source)
- [ ] **Проверка кликабельности** телефонов/адресов на всех страницах
- [ ] **Утренние группы** — проверить, что упомянуты везде, где стоимость/расписание
- [ ] **Подключение форм к нативным формам Tilda** для корректной доставки заявок
- [ ] Финальная проверка всех страниц и предпросмотр владельцу

---

### Сессия 6 (агент — Devin) — AI-бот для MAX

**Дата:** 28 июня 2026
**Репозиторий:** `Dymova-english`, новый сервис в папке `bot/` (Python/FastAPI).
**Итог:** разработан с нуля AI-консультант + менеджер по продажам для мессенджера MAX, развёрнут временно для теста, интегрирован с BigBen CRM и бесплатным LLM (Groq), налажен «цикл улучшения», CI, ежедневный отчёт и админ-команды.

#### Полная история переписки и решений

1. **Запрос владельца.** Прислал ТЗ (30+ пунктов): бот-консультант в MAX с подбором курса, воронкой продаж, базой знаний, интеграцией BigBen CRM, мини-приложением (личный кабинет) и двусторонней связью «бот ↔ приложение». Изначальная формулировка — «скопируй бота из репозитория `foxinburg-ebos` в `dymova-english`».

2. **Уточнение архитектуры.** Объяснил, что `Dymova-english` — репозиторий сайта на Tilda, а `Foxinburg-EBOS` — отдельная платформа (FastAPI+React); сливать их нельзя. Владелец решил: **бота в `foxinburg-ebos` отключить (заглушить webhook), а в `dymova-english` разработать заново** с нуля. Код не копировать.

3. **Выбор ИИ.** Договорились о бесплатном провайдере (не обязательно российском). Перебор: OpenRouter → Google Gemini (не получилось зарегистрировать из РФ) → **Groq** (бесплатно, быстро, без региональных ограничений). Клиент LLM сделан провайдер-агностик (OpenAI-совместимый), переключается через env.

4. **Источники базы знаний.** Данные взяты с сайта https://dymova-english.ru/index-new и подстраниц: 2 филиала (адреса, телефоны, Яндекс.Карты), цены, форматы, курсы (англ./нем./кит./чтение/грамматика/летняя академия), методика, FAQ, акции, соцсети. Бот отвечает только из базы, цены не выдумывает.

5. **Разработка (PR #15).** Каркас: AI Core (намерение → память → поиск по базе → продажи → подбор → запись → передача администратору), мини-приложение, BigBen-клиент. PR #15 влит.

6. **Фикс адреса MAX (PR #17).** `platform-api2.max.ru` требует сертификат Минцифры; рабочий адрес — `botapi.max.ru` (стал значением по умолчанию). Подтверждена личность бота: user_id=299985016, «Фоксинбург», @id611904726658_bot. PR #17 влит.

7. **Секреты.** Запрошены по очереди: `MAX_BOT_TOKEN` (рабочий ✅), `BIGBEN_API_KEY`, затем логин/пароль панели BigBen для поиска недостающих значений.

8. **Интеграция BigBen (PR #18).** Найдено в панели и проверено вживую:
   - эндпоинт `https://panel.bigbencrm.ru/api/leads/add` (GET, «интеграция с сайтом через API»);
   - воронка `pipeline_id=1924` («Бот Макс»), этап `pipeline_status_id=1` («Входящие»; ещё Успешно=5, Отказ=6).
   Тестовая заявка прошла end-to-end (id 379459) и удалена. Неверный ключ → 401.

9. **Возможности BigBen.** Проверено: публичный API BigBen **односторонний** — только приём заявок. Чтения расписания/свободных мест в API нет. Решено усилить заявки вместо ожидания расширенного API.

10. **Обогащение заявок (PR #20).** Добавлены UTM-атрибуция (`utm_*`, fbclid/fbp/fbc), поля email/город, богатый `user_note` (резюме диалога, курс, филиал, возраст). UTM берётся из deep-link `/start` и из мини-приложения.

11. **Временный деплой для теста в реальном MAX.** uvicorn (порт 8000) + публичный HTTPS-туннель cloudflared (протокол http2, т.к. QUIC заблокирован), webhook зарегистрирован в MAX с секретом. Удалён старый webhook EBOS (`foxinburg.ru/...`), чтобы сообщения обрабатывал только новый бот.

12. **Groq подключён.** `LLM_BASE_URL=https://api.groq.com/openai/v1`, модель `llama-3.3-70b-versatile`. Бот заговорил «живо», опираясь на базу знаний.

13. **Обратная связь владельца → правки (PR #21):**
    - бот не знал учебники → добавлены **УМК My Level** (уровни 1–4: возраст, год, CEFR, детали) + FAQ; для 9 лет — My Level 2/3;
    - найден баг: вопрос «…Ребён**ку** 9 лет» распознавался как приветствие (токен «ку» внутри слова) — приветствие переведено на матчинг по границам слов;
    - вопросы про учебники/материалы маршрутизируются в RAG-консультацию, а не в подбор курса.

14. **«Цикл улучшения» (объяснение + реализация, PR #21).** Владельцу разъяснено: бот **не дообучается сам** от переписки (веса модели фиксированы) — управляемое улучшение идёт через базу знаний и промпт. Реализован журнал «пробелов» (`app/insights.py`): слабые ответы (низкое совпадение с базой) пишутся в JSONL без ПДн (`user_id` хэшируется). Отчёт: `GET /admin/insights`, `GET /admin/insights/digest`, `POST /admin/digest/send`.

15. **CI.** `.github/workflows/bot-ci.yml` — `pytest` на push/PR (paths `bot/**`). Проходит ✅.

16. **Ежедневный отчёт.** Планировщик (`app/scheduler.py`) раз в сутки в 21:00 МСК (`DIGEST_HOUR`/`DIGEST_TZ_OFFSET` настраиваемы) шлёт дайджест администраторам в MAX. Реализован как фоновая asyncio-задача через FastAPI `lifespan`.

17. **Администраторы бота.** `ADMIN_MAX_IDS` — конкретные user_id MAX. В чате: `/myid` (узнать свой ID, доступно всем), `/отчёт` (дайджест, только админам). Владелец добавлен: **ID 5897639**. Тестовый дайджест доставлен в MAX, `/отчёт` отвечает.

18. **Консолидация.** Весь сервис бота + CI сведён в `main` отдельным PR (**PR #22**, `devin/1782666001-bot-to-main` → `main`, CI ✅); эта запись журнала добавлена туда же. Промежуточные PR #15/#17/#18/#20/#21 после мерджа #22 можно закрывать.

19. **Артефакты деплоя (PR #23).** В `bot/` добавлены `Dockerfile`, `.dockerignore`, `docker-compose.yml` (бот + Caddy с авто-HTTPS), `deploy/Caddyfile`, `deploy/foxinburg-bot.service` (systemd) и **`bot/DEPLOY.md`** — пошаговая инструкция (Docker и без Docker) + заметки по Yandex Cloud. Ветка `devin/1782666824-bot-deploy`.

20. **План постоянного хостинга (НЕ завершён).** Владелец: сайт `foxinburg.ru` уже работает на ВМ в Yandex Cloud — решено развернуть бота на той же ВМ под поддоменом **`bot.foxinburg.ru`**. ⚠️ Порты 80/443 на ВМ, вероятно, заняты веб-сервером сайта, поэтому НЕ поднимать Caddy-вариант «как есть»: бот должен слушать внутренний порт 8000, а наружу выводиться через уже стоящий веб-сервер (добавить vhost-поддомен `bot.foxinburg.ru` → `localhost:8000` + HTTPS) либо использовать systemd-вариант из `DEPLOY.md`. **Не сломать существующий сайт.** Сначала зайти на сервер и посмотреть, что установлено (nginx/apache/docker), затем выбрать способ.

#### Что осталось сделать (следующему агенту)

- Получить SSH-доступ к ВМ Yandex Cloud (IP, пользователь, ключ/пароль) и подтверждение поддомена `bot.foxinburg.ru` (+ где управляется DNS `foxinburg.ru`).
- Развернуть `bot/` на ВМ: внутренний порт 8000 (systemd `foxinburg-bot.service` или Docker без проброса 80/443), реверс-прокси поддомена через существующий веб-сервер сайта, HTTPS-сертификат.
- Прописать секреты в окружении сервера: `MAX_BOT_TOKEN`, `BIGBEN_API_KEY`, `LLM_API_KEY` (Groq), `MAX_WEBHOOK_SECRET`, `ADMIN_MAX_IDS=5897639`, `MINIAPP_BASE_URL=https://bot.foxinburg.ru/app/`, пути `STATE_FILE`/`INSIGHTS_FILE` в персистентный каталог.
- Зарегистрировать webhook MAX на `https://bot.foxinburg.ru/webhook` (`POST /admin/set-webhook`).
- Проверить: `/health`, живой диалог через Groq, заявка в BigBen (воронка 1924/Входящие), ежедневный отчёт админам в 21:00, команда `/отчёт`.
- Проверить доступность Groq с ВМ; при блокировке — сменить провайдера LLM или поднять исходящий прокси.

> **Примечание:** временный тестовый хостинг (cloudflared-туннель `tvs-tutorials-scratch-metropolitan.trycloudflare.com`) работал только в рамках сессии и после рестарта недоступен — это нормально, на постоянный хостинг он не влияет.

#### Ключевые значения (для передачи между агентами)

- **MAX:** бот @id611904726658_bot («Фоксинбург», user_id=299985016); бизнес-канал https://max.ru/id611904726658_biz; API `https://botapi.max.ru`; заголовок авторизации `Authorization: {token}` (без `Bearer`).
- **BigBen CRM:** панель https://panel.bigbencrm.ru/admin/ ; API `https://panel.bigbencrm.ru/api/leads/add` (GET, write-only); воронка 1924 «Бот Макс», этап «Входящие»=1 (Успешно=5, Отказ=6).
- **LLM:** Groq, `https://api.groq.com/openai/v1`, `llama-3.3-70b-versatile` (бесплатно). Альтернатива — OpenRouter.
- **Админ MAX:** 5897639 (владелец).
- **Секреты (в окружении, не в коде):** `MAX_BOT_TOKEN`, `BIGBEN_API_KEY`, `LLM_API_KEY` (Groq), `MAX_WEBHOOK_SECRET`, опц. `ADMIN_TOKEN`.
- **Деплой:** временный (cloudflared-туннель на время сессии). Для постоянной работы — развернуть `bot/` на сервере с публичным HTTPS, прописать env и зарегистрировать webhook (`POST /admin/set-webhook`).

#### Структура сервиса бота

```
bot/
├── README.md
├── requirements.txt / pyproject.toml
├── .env.example                 # все настройки с комментариями
└── app/
    ├── main.py                  # FastAPI: /webhook, /api/miniapp/*, /admin/*, /health, lifespan
    ├── ai_core.py               # маршрутизация диалога, RAG-консультация, логирование пробелов
    ├── intent.py                # распознавание намерений и сущностей (возраст/телефон/дата)
    ├── sales.py                 # системный промпт, работа с возражениями, sales-nudge
    ├── lead_manager.py          # пошаговый сбор заявки
    ├── course_selector.py       # подбор курса по возрасту/формату
    ├── bigben.py                # отправка лида в BigBen (UTM + контекст)
    ├── max_client.py            # клиент MAX Bot API (сообщения, кнопки, webhook)
    ├── memory.py                # состояние диалога (этап, лид, история, UTM)
    ├── llm.py                   # OpenAI-совместимый LLM-клиент (Groq)
    ├── insights.py              # цикл улучшения: журнал пробелов + дайджест
    ├── scheduler.py             # ежедневный отчёт администраторам
    ├── admin_router.py          # передача диалога администратору
    ├── knowledge/
    │   ├── data.yaml            # база знаний школы (включая учебники My Level)
    │   └── kb.py                # загрузка + лексический поиск (RAG-lite)
    └── miniapp/                 # мини-приложение MAX (витрина, подбор, форма, кабинет)
```

---

### Сессия 7 (агент — Devin) — Контент-правки + адаптив под устройства

**Дата:** 28 июня 2026
**Где работаем:** ТОЛЬКО рабочая копия в Tilda — НЕ трогаем оригиналы `dymova-english.ru/`. Все правки в редакторе Tilda (проект `2053071`), это живой сайт, а не репозиторий. Кастомные блоки `fxb-*` живут внутри страниц Tilda, в репозитории их нет.

**Доступы / ключевые значения:**
- Tilda логин: `vkrivobokova4@gmail.com` (пароль сохранён в секретах сессии). 2FA нет.
- Проект Tilda: `projectid=2053071`. Редактор страницы: `https://tilda.ru/page/?pageid=<PAGEID>&projectid=2053071`.
- Рабочая копия главной: `/index-new`, `pageid=151210576` → live `https://dymova-english.ru/index-new`.

**Часть A — 23 контент-правки на /index-new (ВЫПОЛНЕНО, опубликовано).**
Внесены все 23 пункта из ТЗ владельца (шапка с одной кнопкой + выпадающий список летних программ; слоган; старт занятий; «Почему нас выбирают»; карточки «Команда профессионалов» и «Качественная обратная связь» + попап «Посмотреть пример» с 3 пустыми окнами под скриншоты; мобильное приложение — чат с педагогом; +2 преимущества (Носитель языка, Помощь с ДЗ); направления и возрасты (2-6/7-11/12-17); «комфортный старт»; пробный урок 1125 руб; «Поехали!»; «Команда, которой вы доверяете»; «Где проходят занятия»; прайс: офлайн по центру/популярный, 8200/9000 руб, +3 карточки (Диагностика 0 руб, Малыши 2-3, Индивидуальные), «оплата мат. капиталом»; отзывы — заголовок и описание; FAQ — 3 ответа; п.23 лицензия — возвращены РЕАЛЬНЫЕ данные оригинала, внутри ничего не менять).
- Осталось от владельца: загрузить картинки в плейсхолдеры попапа «Посмотреть пример» (п.6) и фото в сетку «Где проходят занятия» (п.17).

**Часть B — адаптив + бургер + плашки онбординга (ВЫПОЛНЕНО на /index-new).**
Найдены 2 бага и исправлены CSS-правками прямо в блоках Tilda (ACE-редактор):
1. **Бургер-меню на планшете/телефоне не открывалось.** Причина: `.fxb-nav` имеет `position:absolute;top:100%`, но у родителя `.fxb-topline` не было `position:relative` → меню уезжало за пределы шапки. На главной шапка внутри блока с `id="fxb-hero"`.
2. **Плашки онбординга** (Бесплатно/Удобный график/1125 руб/Поехали!) висели на разной высоте. Сделали `.fxb-card` флекс-колонкой, плашке `.fxb-tag` → `margin-top:auto;align-self:center` (по центру вниз карточки).
- Блоки на главной: шапка/герой `rec2421794631` (#fxb-hero), онбординг `rec2422576651`.
- Проверено live на 1280/800/430 px: бургер открывается под шапкой, пункты и подменю по центру, плашки ровно по ряду, горизонтального скролла нет. Записано видео.

**Часть C — тот же фикс шапки на ПОДСТРАНИЦАХ (В ПРОЦЕССЕ, остановлено владельцем).**
Подстраницы используют ОБЩИЙ блок-шапку (первый HTML-блок на странице, комментарий «БЛОК: ЕДИНАЯ ШАПКА (НАВИГАЦИЯ) — для всех страниц, кроме главной»), там `id="fxb-shapka"` (на главной было `fxb-hero`!). У каждой страницы своя копия блока — нужно править каждую отдельно. Один медиа-запрос `@media(max-width:960px)` (покрывает и планшет, и телефон).

Точечные замены в ACE-редакторе шапки (через консоль браузера `ace.edit(document.querySelectorAll('.ace_editor')[0]`)):
- `#fxb-shapka .fxb-topline{...padding:16px 0 14px}` → добавить `;position:relative` перед `}`.
- mobile-nav: строку с `border-radius:0 0 16px 16px}` → `...16px 16px;align-items:center;text-align:center}` и добавить правило `#fxb-shapka .fxb-dd{width:100%}`.
- `#fxb-shapka .fxb-nav>a,#fxb-shapka .fxb-dd-btn{...;text-align:left}` → `...;text-align:center;justify-content:center}`.
Процедура на странице: открыть редактор `pageid` → навести на 1-й блок → «Контент» (верхняя левая панель) → выполнить замены в консоли (проверять, что каждая искомая строка встречается ровно 1 раз) → «Сохранить и закрыть» → «Опубликовать» → «Закрыть».

Статус по подстраницам (фикс шапки применён И опубликован):
- [x] /index-new          151210576  (главная-копия, + плашки онбординга)
- [x] /podrostki          151228746
- [x] /doshkolniki        151228606
- [x] /mladshie-shkolniki 151228676
- [x] /kontakty           151228006
- [x] /letnyaya-akademiya 151229566
- [x] /online-zanyatiya   151229606
- [x] /podderzhivayushchie-online 151229676  (Сессия 8)
- [x] /standartnye-offline 151229756  (Сессия 8)
- [x] /reading-new        151292376  (Сессия 8)
- [x] /grammar-new        151292406  (Сессия 8)
- [x] /preparation-new    151292476  (Сессия 8)
- [x] /novosti            151324806  (Сессия 8)
- [x] /vakansii           151324866  (Сессия 8)

НЕ ТРОГАТЬ (оригиналы/старые/служебные): `/` 32889798, `/preparation` 130390566, `/news` 132317976, `/lmsfoxinburg` 135877726, `/reading` 137726126, `/grammar` 137739566, `/vacant` 146080046, `/thanks` 11025377, `/policy` 11026209, `/oshibka` 11025828.

**Часть C завершена в Сессии 8** — все 14 подстраниц имеют исправленную шапку.

---

### Сессия 8 (агент — Devin) — Завершение фикса шапки на подстраницах

**Дата:** 28 июня 2026
**Где работаем:** Tilda (проект `2053071`), 7 оставшихся подстраниц из Сессии 7.

**Задача:** доделать CSS-фикс бургер-меню шапки (`#fxb-shapka`) на 7 подстраницах, где правки НЕ были применены в Сессии 7.

**Выполнено:**
1. Залогинились в Tilda (`vkrivobokova4@gmail.com`).
2. Для каждой из 7 подстраниц автоматизировали через Playwright + CDP:
   - Открытие редактора страницы → вызов `edrec__editRecordContent(recordId)` → активация ACE-редактора.
   - Применение 4 CSS-правок в ACE через `ace.edit()` + `editor.setValue()`:
     1. `position:relative` в `.fxb-topline` (фикс позиционирования навигации).
     2. `align-items:center;text-align:center` в мобильном контейнере навигации.
     3. `#fxb-shapka .fxb-dd{width:100%}` (подменю на всю ширину).
     4. `text-align:center;justify-content:center` для пунктов меню и кнопок подменю.
   - Сохранение («Сохранить и закрыть») + публикация («Опубликовать»).
3. Все 7 страниц успешно исправлены и опубликованы:
   - `/podderzhivayushchie-online` (151229676) — record 2422786271
   - `/standartnye-offline` (151229756) — record 2422786401
   - `/reading-new` (151292376) — record 2422858241
   - `/grammar-new` (151292406) — record 2422858931
   - `/preparation-new` (151292476) — record 2422860451
   - `/novosti` (151324806) — record 2423334301
   - `/vakansii` (151324866) — record 2423334801
4. Проверка бургер-меню на мобильной ширине (430px) на 3 страницах:
   - `/podderzhivayushchie-online` — бургер открывается, пункты центрированы.
   - `/novosti` — бургер открывается, пункты центрированы.
   - `/vakansii` — бургер открывается, пункты центрированы.
   - Горизонтального скролла нет. Записано видео-подтверждение.

**Итог Части C (Сессия 7 + Сессия 8):** все 14 подстраниц (+ главная /index-new) имеют исправленный бургер-меню. Фикс шапки полностью завершён.

**Доп. фикс: компактность «Летние программы» на мобильных.**
По обратной связи владельца: блок с тегами летних программ (`.fxb-summer-tags`) занимал слишком много места и мог выходить за границы экрана на узких телефонах.
Исправлено на всех 13 подстраницах (`#fxb-shapka`) — в `@media(max-width:960px)`:
- `.fxb-summer` — `padding:6px 0 2px;max-width:100%` (было `padding:10px 0 4px`).
- `.fxb-summer-tags` — `gap:3px;flex-wrap:wrap;max-width:100%` (было `gap:4px`).
- `.fxb-summer-tags a` — `font-size:10px;padding:3px 6px` (было `font-size:11px;padding:4px 10px`).
Результат: теги компактно помещаются в 2 строки даже на 360px-экранах (iPhone SE), горизонтального переполнения нет.

---

### Сессии 9–13 (агент — Devin) — Итерации MAX-бота (PR #28–#45)

**Период:** 28–30 июня 2026
**Где работаем:** сервис `bot/` (FastAPI). Каждый пункт — отдельный PR в `main`, CI (`pytest -q`) зелёный. Прод после мерджа переразворачивался с `main` (ВМ Yandex Cloud, бот «Фоксинбург» в MAX). История восстановлена из git и переписки с владельцем — для бесшовной передачи.

#### Качество базы знаний и продаж
- **PR #28** — фикс ранжирования поиска по базе: бонус за совпадение заголовка, smalltalk вынесен из KB (`intent.py`, `kb.py`, `data.yaml`).
- **PR #31** — исправлено отчество основателя в юр-названии → «Александровна» (`data.yaml`, `kb.py`).
- **PR #37** — усиление воронки продаж: валидация лида, обработка жалоб, адреса филиалов в ответах, языковой фильтр (ответ только на русском) (`ai_core.py`, `intent.py`, `lead_manager.py`, `llm.py`, `sales.py`, тест `test_quality_fixes.py`).

#### Админ мини-приложение
- **PR #29** — конструктор рассылок (broadcast) в админ-странице мини-приложения, только для админов (`broadcast.py`, `miniapp/admin.html/js`).
- **PR #30** — вкладка «Пользователи» в админке: полная история диалога + экспорт.
- **PR #33** — поля лида показаны колонками в таблице пользователей.
- **PR #32** — обращения «нужен администратор» маршрутизируются админу нужного филиала (`main.py`, `data.yaml`, тест `test_branch_admin.py`).

#### Помощник по домашним заданиям (фото → разбор)
- **PR #34** — функция «помощник по ДЗ»: фото задания → разбор через vision-модель; добавлен `python-multipart` для CI/Docker (`llm.py`, `main.py`, `miniapp/`).
- **PR #35** — разрешён выбор фото ДЗ из галереи/файлов (убрана принудительная камера).
- **PR #36** — разбираются ВСЕ задания на фото, а не только первое (`llm.py`, `main.py`, тест `test_homework.py`).

#### Групповой режим (ответы в групповых чатах)
- **PR #38** — групповой режим: бот отвечает по базе знаний при упоминании/ответе; исправлен парсинг идентичности бота для «плоского» ответа `/me` (`group_chat.py`, `max_client.py`, `main.py`, тест `test_group_chat.py`). Имя бота в MAX — **«Фоксинбург»** (не «Фокси»); MAX доставляет сообщение боту только при реальном @-упоминании (выбор из списка) или ответе на его сообщение.
- **PR #39** — групповые ответы только из базы (KB-only).
- **PR #40** — групповые ответы снова AI-first, но с ограждениями: только русский, 2–4 предложения, только KB + факты филиала, без продаж и сбора лидов в общем чате.
  > Важно про MAX: групповой режим включается списком `GROUP_CHAT_WHITELIST` в `.env` на проде. По решению владельца выбран **вариант Б** — открыть для всех чатов, куда добавлен бот (whitelist очищается на проде). Это правка `.env`, не кода.

#### Заявки, уведомления, персонализация
- **PR #41** — мини-приложение предзаполняет данные заявки из выбранного предложения (курс / смена летней академии / филиал / формат); те же детали идут в заметку BigBen CRM (`bigben.py`, `memory.py`, `miniapp/`, тест `test_bot.py`).
- **PR #42** — пакет улучшений: уведомление админу в личку о каждой новой заявке из мини-аппа (всем из `ADMIN_MAX_IDS`, параллельно с CRM); более тёплый групповой тон; усилён сбор лида на «грязных» ответах (телефон с точками/скобками, имя из «Иванова Анна, ребёнку 9», вперемешку дата/возраст/филиал); **убран временный отладочный лог «GROUP MSG…»** (больше не пишет тексты групповых сообщений в логи).
- **PR #43** — постоянная «карточка клиента»: бот помнит между сессиями имя/возраст ребёнка, интересующий курс/смену, формат, филиал, прошлое возражение, была ли заявка; не переспрашивает известное; узнаёт вернувшегося клиента (12ч+) и предлагает продолжить. Память сохраняется на диск (`memory.py`, `ai_core.py`, `sales.py`, тест `test_client_card.py`).
- **PR #44** — **собеседник = РОДИТЕЛЬ**: общение строится с родителем, имя ребёнка используется только при упоминании ребёнка («как у Миши с английским»), а не для прямого обращения к ребёнку. Также `.strip()` ключа модели — пробел в `LLM_API_KEY` ронял httpx и бот молча оставался без ИИ (`ai_core.py`, `llm.py`, `memory.py`, `sales.py`).
- **PR #45** — полировка диалога по итогам прогона 6 живых сценариев (OpenRouter), 4 фикса (`ai_core.py`, `lead_manager.py`, `llm.py`, тест `test_dialogue_fixes.py`):
  - **A** — английские слова-вставки в русском ответе (`indeed/actually/however…`) вырезаются, бренд-токены (My Level, Hippo, Foxinburg) и полностью английские ответы не трогаются.
  - **B** — на шаге телефона прислали дату → «Дату рождения записал ✅ Остался телефон…» (вместо «номер указан не полностью»).
  - **C** — не два вопроса подряд во время сбора заявки: `during_lead=True` (отвечай кратко, не спрашивай) + детерминированная зачистка встречного вопроса (`_drop_trailing_question`).
  - **D** — разнообразный хэндофф жалоб (`_HANDOFF_FOLLOWUPS` по числу ответов) + отдельный ответ на «хочу руководителя» (`_wants_manager`).
  - Проверка: `pytest -q` → 72 passed.

---

### Сессия 9 (агент — Devin) — Надёжность бота 24/7 (Этап 1) — PR #90

**Дата:** 2026-07-04
**PR:** #90 — feat(bot): надёжность LLM 24/7 — каскад провайдеров, ретраи, дедуп вебхука (`devin/1783203969-bot-reliability-main` → main)
**Запрос владельца:** «бот уже есть, хочу чтобы работал надёжно 24/7, а то ИИ отключается; общение как с живым человеком». Затем: «давай что-то подключим, что можно оплачивать из России».

**Что сделано:**
- `bot/app/llm.py`: каскад провайдеров (основной `LLM_API_KEY/BASE_URL/MODEL` + `LLM_FALLBACKS` JSON), ретраи на 429/5xx/сеть с экспоненциальной паузой, один общий `httpx.AsyncClient`. `complete()` сохраняет проверку `_mostly_russian`, `complete_vision()` идёт тем же каскадом. Раньше был один провайдер, без ретраев, новый клиент на каждый запрос → при сбое молча падал на шаблоны (это и была причина «ИИ отключается»).
- `bot/app/config.py`: поле `LLM_FALLBACKS`.
- `bot/app/dedup.py` (новый): дедуп вебхук-ивентов (SQLite рядом со `STATE_FILE` или `DEDUP_FILE`, in-memory LRU как фолбэк).
- `bot/app/main.py`: вебхук обрабатывает апдейты в фоне (`asyncio.create_task`) с изоляцией ошибок и дедупом; `/health` расширен (`version`, `llm_configured`, `llm_providers`), добавлен `/ready`.
- `bot/.env.example`: ProxyAPI.ru как основной провайдер (оплата из РФ) + пример каскада.

**Как проверено:** `cd bot && pytest -q` → 75 passed. Прод после деплоя: `/health` = ok, `llm_providers:2`, живой ответ ИИ естественный (Phonics/TPR, цена пробного 1125 ₽, встречный вопрос про возраст).

**Решения и нюансы:** GitHub default-ветка репо — `devin/1782567621-foxinburg-prompt` (упрощённый бот), но ПРОД развёрнут из `main` (полноценный бот: ДЗ/vision, группы, admin_router, рассылки, CRM). Ранее Этап 1 по ошибке собрали на default-ветке (PR #88/#89) — сюда перенесли на `main`. Память диалогов оставлена на JSON-персистентности (`STATE_FILE`), не переписывали на SQLite (риск).
**Деплой:** ДА. VPS `yc-user@89.169.132.104`, `~/Dymova-english/bot`, `docker compose -f docker-compose.prod.yml up -d --build`. `.env`: основной провайдер — ProxyAPI (`https://api.proxyapi.ru/openai/v1`, `gpt-4o-mini`, `VISION_MODEL=gpt-4o-mini`), запас в `LLM_FALLBACKS` — прежний OpenRouter. Секреты: `PROXYAPI_KEY`, `MAX_BOT_TOKEN`, `VPS_SSH_PRIVATE_KEY_OWNER` (все org/user-scoped). Вебхук `https://bot.dymova-english.ru/webhook` уже зарегистрирован.
**Осталось / следующий шаг:** сквозной тест в живом MAX-чате (на стороне владельца). Далее Этап 2 (живее диалог) и Этап 3 (единый бот: Telegram + виджет на сайте).

---

### Сессия 10 (агент — Devin) — Помощник по ДЗ: учить, а не решать — PR #91

**Дата:** 2026-07-04
**PR:** #91 — feat(bot): помощник по ДЗ учит, а не решает за ученика (`devin/1783205197-homework-tutor` → main)
**Запрос владельца:** «доработай работу с домашним заданием — помочь и чётко объяснить, что нужно делать, какое задание и пример, наглядно; но НЕ решать всё самому — подсказать, научить, помочь».

**Что сделано:**
- `bot/app/main.py` (`_homework_system_prompt`/`_homework_user_prompt`): по каждому заданию с фото — 1) что нужно сделать, 2) как сделать (правило + шаги), 3) пример на ДРУГОМ похожем слове/предложении. Строго запрещено давать готовые ответы на пункты и решать за ребёнка. Раньше промпт требовал «правильный пример или ответ».
- `bot/app/miniapp/index.html`: копирайт блока «Помощь с домашкой» под новое обещание.
- `bot/tests/test_homework_tutor.py`: регрессия на ограничения.

**Как проверено:** `cd bot && pytest -q` → 78 passed.
**Решения и нюансы:** сохранён разбор ВСЕХ заданий на фото по очереди; `complete_vision` идёт каскадом (Этап 1), но vision работает надёжно на основном провайдере (ProxyAPI gpt-4o-mini).
**Деплой:** после мёрджа #91 в `main` — редеплой прод-стека `bot` тем же способом, что в Сессии 9.
**Осталось / следующий шаг:** тест мини-приложения (загрузка фото ДЗ) после деплоя; Этапы 2–3.

---

### Сессия 11 (агент — Devin) — Маршрутизация ДЗ в мини-апп + запись диалогов + подробный отчёт 21:00 — PR #92

**Дата:** 2026-07-04
**PR:** #92 — feat(bot): маршрутизация ДЗ в мини-апп, запись диалогов и подробный отчёт 21:00 (`devin/1783205802-homework-routing-report` → main)
**Запрос владельца:** «бот помогает и должен перебрасывать на мини-приложение помощь с домашкой — эта услуга бесплатная; анализируй и записывай все логи; в отчётах в 21:00 нужна подробная инфа — кто и что писал, какой результат».

**Что сделано:**
- `bot/app/intent.py`: намерение `HOMEWORK` + `is_homework_request()` (домашк/ДЗ/«помоги с задани»/«номер N …»); токен «дз» — по границам слова (`\bдз\b`), чтобы не ловить «надзор»/«подземный».
- `bot/app/main.py`: при `HOMEWORK` бот отвечает коротко, что разбор ДЗ **бесплатный**, и даёт кнопку в мини-апп (`#homework`) — без LLM; кнопка «📸 Помощь с домашкой (бесплатно)» в `_main_menu()` и в контекстных кнопках; `log_turn(...)` в ветках homework/handoff/общий диалог; `/отчёт` шлёт conversations digest + insights digest.
- `bot/app/convlog.py` (новый): JSONL-лог диалогов на volume через `CONV_LOG_FILE` (иначе рядом со `STATE_FILE`; без пути — no-op). Поля `{ts, user_id, user_text, bot_reply, intent, stage, result}`.
- `bot/app/conv_report.py` (новый): `conversations_digest(days)` — сводка (диалогов/сообщений, заявок, передано админам, домашка, консультации) + по пользователям `user_id` (+имя из памяти), результат и выжимка сообщений.
- `bot/app/scheduler.py`: ежедневный отчёт = conversations digest, затем insights digest.
- `bot/app/config.py` + `bot/.env.example`: `CONV_LOG_FILE`.
- `bot/tests/test_homework_routing_report.py`: intent HOMEWORK, граница «дз», роутинг с кнопкой мини-аппа, запись convlog + отражение в conversations_digest.

**Как проверено:** `cd bot && python3 -m pytest -q` → 78 passed.
**Решения и нюансы:** лог диалогов — отдельный JSONL, чтобы не вмешиваться в JSON-персистентность `STATE_FILE`; homework-ветка не пишется в LLM-контекст диалога (чистый редирект). На проде отдельно устранён `BigBen 401`: подставлен новый `BIGBEN_API_KEY`, тест-лид → HTTP 200 (в CRM тест-лид «ТЕСТ DEVIN удалить»).
**Деплой:** после мёрджа #92 — редеплой прод-стека `bot` из `main`; добавить `CONV_LOG_FILE=/data/conversations.jsonl` в прод `.env`, чтобы включить запись диалогов и подробный отчёт 21:00.
**Осталось / следующий шаг:** редеплой из `main`, включить `CONV_LOG_FILE`, проверить отчёт `/отчёт`; Этапы 2–3.

---

### Сессия 12 (агент — Devin) — Этап 2: живой диалог (меньше шаблонов, длиннее память)

**Дата:** 2026-07-04
**PR:** feat(bot): живой диалог — приветствия через LLM, длиннее память контекста (`devin/1783241981-bot-livelier-dialogue` → main)
**Запрос владельца:** «да делаем!» — согласие на Этап 2 (диалог живее, меньше шаблонности; бот сам ведёт беседу и задаёт вопросы — SPIN уже в промпте).

**Что сделано (точечно, без рефакторинга):**
- `bot/app/memory.py`: `MAX_HISTORY` 12 → 20 (дольше помним переписку).
- `bot/app/config.py`: новое поле `LLM_HISTORY_TURNS: int = 16` — сколько последних сообщений уходит в LLM-контекст.
- `bot/app/ai_core.py`: в `_consult_with_context` срез истории `conv.history[-8:]` → `conv.history[-settings.LLM_HISTORY_TURNS:]`; удалён жёсткий `_smalltalk_reply` — приветствия/болтовня теперь ВСЕГДА идут через `_consult()` (LLM отвечает по-разному, не шаблоном). Тёплый fallback-ответ сохранён на случай, если LLM пустой/недоступен.
- Промпт продаж (`sales.py`) не менялся — SPIN-логика уже была.

**Как проверено:** `cd bot && python3 -m pytest -q` → 82 passed. Новые/обновлённые тесты: greeting/smalltalk fallback при выключенном LLM; регрессия на срез истории по `LLM_HISTORY_TURNS`; обновлены ожидания `MAX_HISTORY=20`.
**Решения и нюансы:** без изменения лид-менеджера/CRM/homework — риск для сбора заявок нулевой. Живость достигается за счёт того, что приветствия и small-talk генерирует LLM (с усиленным памятью-контекстом), а не захардкоженные строки.
**Деплой:** после мёрджа — редеплой прод-стека `bot` из `main` тем же способом. Опционально можно поднять `LLM_HISTORY_TURNS` в прод `.env`.
**Осталось / следующий шаг:** Этап 3 — единый бот: Telegram-адаптер + чат-виджет на сайте (одно ядро, единые лиды/знания).

---

### Сессия 13 (агент — Devin) — Этап 3 (канал 1): чат-виджет на сайте

**Дата:** 2026-07-04
**PR:** feat(bot): чат-виджет на сайте — тот же «мозг» на dymova-english.ru (`devin/1783242369-web-chat-widget` → main)
**Запрос владельца:** Этап 3 «единый бот на всех платформах» — начали с виджета на сайте (Тильда): один и тот же диалог/знания/лиды, что и в MAX.

**Что сделано (только `bot/`):**
- `bot/app/main.py`: новый `POST /api/chat` — вход `{session_id?, text}`, внутренний `user_id = web:<session_id>` (веб-диалоги отдельны от MAX). Роутинг как в MAX: `HOMEWORK` → короткий ответ + кнопка мини-аппа (без LLM), иначе `handle_message()`. Логирование через `convlog.log_turn`. Ответ `{session_id, reply, buttons:[{title,url}]}`. Валидация: пустой/слишком длинный text → 400. Общая классификация результата вынесена в helper `_dialogue_result()` и переиспользована в MAX-ветке (DRY). Конвертация link-кнопок → `_response_buttons()`. Добавлен `CORSMiddleware` (origin'ы сайта + `localhost:*`). Статика виджета монтируется на `/widget`.
- `bot/app/config.py` + `bot/.env.example`: `WEB_CHAT_ORIGINS` (CSV; дефолт — `dymova-english.ru`, `www.`, `localhost:*`).
- `bot/app/widget/foxi.js` (новый): самодостаточный встраиваемый виджет (ванильный JS, без зависимостей). Плавающая кнопка-🦊 + панель чата, `session_id` в localStorage, base URL из `document.currentScript.src`, поддержка кнопок-ссылок, адаптив под мобильные. Встраивание на сайт: `<script src="https://bot.dymova-english.ru/widget/foxi.js"></script>`.
- `bot/tests/test_web_chat.py` (новый): валидация text→400, генерация/переиспользование `session_id`, homework-кнопка, устойчивость при выключенном LLM.

**Как проверено:** `cd bot && python3 -m pytest -q` → 84 passed; `node --check foxi.js` — ок; ручная проверка `/api/chat` через TestClient (200, есть `session_id`+`reply`).
**Решения и нюансы:** веб-канал использует то же ядро `ai_core.handle_message`, поэтому знания/продажи/логи/отчёт 21:00 общие; веб-пользователи видны в отчёте с префиксом `web:`. Хэндофф к админам всё так же уходит в MAX (админы там).
**Отдельно:** по просьбе владельца выполнен полный сброс Telegram-бота **@foxinburg_bot** (id 8211576531): очищены описание/short description, удалены команды (все scope), снят вебхук с `drop_pending_updates`.
**Деплой:** после мёрджа — редеплой прод-стека `bot` из `main`; затем вставить `<script src="https://bot.dymova-english.ru/widget/foxi.js"></script>` на сайт Тильда. При необходимости уточнить `WEB_CHAT_ORIGINS` в прод `.env`.
**Осталось / следующий шаг:** Этап 3 (канал 2) — Telegram-адаптер на том же ядре (токен `TELEGRAM_BOT_TOKEN` уже сохранён).

---

### Сессия 14 (агент — Devin) — Этап 3 (канал 2): Telegram-адаптер

**Дата:** 2026-07-04
**PR:** feat(bot): Telegram-адаптер — тот же «мозг» в @foxinburg_bot (`devin/1783242923-telegram-adapter` → main)
**Запрос владельца:** Этап 3 «единый бот» — подключить Telegram (@foxinburg_bot) к тому же ядру; токен сохранён как секрет `TELEGRAM_BOT_TOKEN`.

**Что сделано (только `bot/`):**
- `bot/app/telegram_client.py` (новый): клиент Telegram Bot API по образцу `max_client` — `send_message(chat_id, text, buttons)` (inline-кнопки-ссылки через `reply_markup.inline_keyboard`; `_normalize_buttons` принимает и `{text,url}`, и `{title,url}`, callback-кнопки без url отбрасываются), `set_webhook(url, secret)`, `delete_webhook()`, singleton `get_telegram()` (включён только при заданном токене).
- `bot/app/main.py`: `POST /telegram/webhook` — проверка заголовка `X-Telegram-Bot-Api-Secret-Token` (403 при несовпадении, если секрет задан), быстрый 200 + фоновая обработка (`_BACKGROUND_TASKS`), дедуп по `tg:<update_id>` через `dedup`. `_process_telegram_update`: `user_id = tg:<chat_id>`; `/start [param]` → `handle_start`; `HOMEWORK` → бесплатный ответ + кнопка мини-аппа; `HANDOFF` → уведомление админов **через MAX-клиент** (`_admin_contact_flow(get_max(), conv)`), ответ пользователю — через telegram_client; иначе — `handle_message` + контекстные кнопки. `log_turn` во всех ветках. Нетекстовые апдейты игнорируются без падения. Добавлен `telegram_configured` в `/health` и `POST /admin/telegram/set-webhook` (защита `_check_admin`, регистрирует вебхук на `TELEGRAM_WEBHOOK_URL` c секретом).
- `bot/app/config.py` + `bot/.env.example`: `TELEGRAM_BOT_TOKEN`, `TELEGRAM_WEBHOOK_SECRET`, `TELEGRAM_WEBHOOK_URL`.
- `bot/tests/test_telegram_adapter.py` (новый): защита секретом (403), обычный текст → send_message с reply, `/start`, homework-кнопка, дедуп по `update_id`, `/admin/telegram/set-webhook`, и (ключевое) `test_telegram_handoff_uses_max_for_admins` — при HANDOFF админ уведомляется через MAX-клиент (admin MAX-id), а ответ пользователю — через telegram_client.

**Как проверено:** `cd bot && python3 -m pytest -q` → 90 passed. Сети в тестах замоканы.
**Решения и нюансы:** админы сидят в MAX, поэтому хэндофф из любого канала (MAX/веб/Telegram) уведомляет их через MAX-клиент; кросс-канальный ответ админа Telegram-пользователю — ограничение платформы, вне объёма. Namespacing user_id: MAX «как есть», веб `web:`, Telegram `tg:` — все видны в едином логе/отчёте 21:00.
**Деплой:** после мёрджа — редеплой прод `bot` из `main`; в прод `.env` добавить `TELEGRAM_BOT_TOKEN`, `TELEGRAM_WEBHOOK_SECRET` (случайная строка) и `TELEGRAM_WEBHOOK_URL=https://bot.dymova-english.ru/telegram/webhook`; затем дернуть `POST /admin/telegram/set-webhook` для регистрации вебхука.
**Осталось / следующий шаг:** деплой Telegram на прод + регистрация вебхука; вставка виджета на сайт Тильда.

---

### Сессия 15 (агент — Devin) — Telegram: исходящий прокси (api.telegram.org заблокирован с VPS)

**Дата:** 2026-07-04
**PR:** feat(bot): поддержка исходящего прокси для Telegram Bot API (`devin/1783244161-telegram-proxy` → main)
**Запрос владельца:** «подключай Telegram через прокси» → затем «найди/возьми платный прокси». Итог: владелец купил SOCKS5-прокси (Proxy6-подобный), строка вида `IP:порт:логин:пароль`.

**Проблема:** после деплоя Telegram-адаптера (Сессия 14) обнаружено, что с прод-VPS (Yandex Cloud, РФ) исходящие вызовы к `https://api.telegram.org` таймаутятся (`curl -m12` → 000/exit 28; в контейнере `httpcore.ConnectTimeout`). Общий HTTPS-egress работает (LLM через ProxyAPI отвечает) — блокируется именно диапазон Telegram (РКН). Входящие вебхуки Telegram→VPS проходят. Значит бот получает сообщения, но не может отправить ответ. Вебхук был снят, чтобы не было «немых» ответов.

**Что сделано (только `bot/`):**
- `bot/app/config.py`: добавлен `TELEGRAM_PROXY_URL: str = ""` (пусто = напрямую).
- `bot/app/telegram_client.py`: helper `_client_kwargs()` — если `TELEGRAM_PROXY_URL` задан, передаёт `proxy=...` в `httpx.AsyncClient`. Применён во всех 3 методах (`send_message`, `set_webhook`, `delete_webhook`). Прокси только для Telegram-клиента (max_client/llm не тронуты). httpx 0.28.1 → параметр `proxy=`.
- `bot/requirements.txt`: `httpx>=0.26,<1.0` → `httpx[socks]>=0.26,<1.0` (для `socks5://` нужен `socksio`).
- `bot/.env.example`: задокументирован `TELEGRAM_PROXY_URL` (пример `socks5://user:pass@host:port`).
- `bot/tests/test_telegram_adapter.py`: юнит-тесты, что при заданном `TELEGRAM_PROXY_URL` клиент создаётся с `proxy=...`, а без него — без; параметризованы по всем 3 методам.

**Как проверено:**
- тесты: `cd bot && python3 -m pytest -q` → 94 passed.
- живая проверка прокси с VPS: `curl --socks5-hostname <user>@<ip:port> https://api.telegram.org/` → 302 за ~1 сек (и HTTP CONNECT тоже 302). До прокси — таймаут.

**Решения и нюансы:** MTProto-прокси (`tg://proxy?...&secret=...`) НЕ подходит — это протокол для клиентов Telegram, а не для Bot API по HTTPS; нужен именно SOCKS5/HTTP. Бесплатные публичные прокси отвергнуты как нестабильные. Прокси-строка хранится в прод `.env` (`TELEGRAM_PROXY_URL`), не коммитится.
**Деплой:** после мёрджа — редеплой прод `bot` из `main` (нужен ребилд ради `httpx[socks]`); в прод `.env` добавить `TELEGRAM_PROXY_URL=socks5://<user:pass>@<ip:port>`; затем `setWebhook` (url + secret_token) и сквозной тест в @foxinburg_bot.
**Осталось / следующий шаг:** сквозной живой тест в Telegram на стороне владельца; прокси бесплатным не заменять — при протухании платного Telegram снова замолчит.

---

### Сессия 16 (агент — Devin) — Telegram: переход с webhook на long-polling (inbound тоже заблокирован)

**Дата:** 2026-07-05
**PR:** feat(bot): Telegram long-polling через прокси (`devin/1783249695-telegram-polling` → main)
**Запрос владельца:** после включения прокси и вебхука бот всё равно «молчит».

**Диагноз:** прокси решил OUTBOUND (VPS→api.telegram.org). Но `getWebhookInfo` показал `last_error_message: "Connection timed out"` и `pending_update_count>0` — Telegram НЕ может доставить вебхук на наш прод-VPS (РФ): INBOUND от серверов Telegram к российскому IP тоже блокируется. Сообщения владельца висели в очереди у Telegram, до бота не доходили. (MAX-вебхук работает, т.к. серверы MAX в РФ.)

**Решение:** переключить Telegram-канал с webhook на long-polling — бот сам тянет апдейты `getUpdates` через тот же SOCKS5-прокси (исходящее соединение работает), обходя блокировку входящего. Вебхук снят (`deleteWebhook`).

**Что сделано (только `bot/`):**
- `bot/app/telegram_client.py`: `get_updates(offset, timeout=25)` — long-poll `getUpdates` через прокси (`_client_kwargs(timeout)` теперь принимает таймаут; httpx-таймаут = telegram-таймаут + 15, иначе httpx рвёт соединение раньше); при не-200/ошибке → `[]` (warning, без падения).
- `bot/app/main.py`: `_telegram_poll_loop(telegram_client)` — сначала `delete_webhook()`, затем бесконечный цикл `get_updates` → диспетч каждого апдейта через существующий `_schedule_telegram_update` (дедуп `tg:<update_id>` + фоновая обработка), `offset=update_id+1`; при ошибке — лог + `sleep(3)` и продолжение; корректная реакция на `CancelledError`. Запуск/остановка в `_lifespan` при `TELEGRAM_POLLING=true` и настроенном клиенте (на shutdown таск отменяется и дожидается). Webhook-эндпоинт оставлен как есть.
- `bot/app/config.py` + `bot/.env.example`: `TELEGRAM_POLLING: bool = False` (на прод-РФ ставим `true`).

**Как проверено:** тесты `cd bot && python3 -m pytest -q` → 97 passed (добавлены тесты `get_updates` и одной итерации poll-loop с выходом по `CancelledError`).
**Решения и нюансы:** webhook в РФ для Telegram нежизнеспособен (двусторонняя блокировка) — polling обязателен. Дедуп переиспользован из webhook-пути, поэтому двойной обработки нет. Прокси нужен и для polling (getUpdates идёт через него).
**Деплой:** редеплой прод `bot` из `main`; в прод `.env` `TELEGRAM_POLLING=true` (+ уже есть `TELEGRAM_PROXY_URL`, `TELEGRAM_BOT_TOKEN`); вебхук снят. Живой тест в @foxinburg_bot.
**Осталось / следующий шаг:** живой тест владельца; следить за сроком платного прокси (истечёт — Telegram снова замолчит).

---

### Сессия 17 (агент — Devin) — Возврат недозаявок: тёплые напоминания (nudge) — PR #99

**Дата:** 2026-07-06
**PR:** #99 — feat(bot): тёплые напоминания — возврат недозаявок (`devin/1783365381-nudge-rebase` → main)
**Запрос владельца:** п.№ 3 из плана — «Возврат недозаявок: кто начал и не закончил — тёплое напоминание через 1–2 дня». Все изменения — сразу на всех каналах (MAX, Telegram, виджет).

**Что сделано (только `bot/`):**
- `bot/app/nudge.py` (новый): модуль тёплых напоминаний. `is_nudgeable(conv)` — проверка: этап discovery/selection/objection/lead, nudge ещё не отправлен, не web-пользователь, неактивность ≥ `NUDGE_DELAY_HOURS` (36ч) и ≤ `NUDGE_MAX_AGE_HOURS` (14д), есть хотя бы одно сообщение пользователя. `compose_message(conv)` — персонализированное сообщение от Фокси 🦊 с учётом имени родителя, ребёнка, курса, филиала + предложение бесплатной диагностики. `send_nudge(conv)` — отправка через нужный канал: MAX-клиент для MAX-пользователей, Telegram-клиент (через прокси) для `tg:*`-пользователей, web пропускается. `run_nudges()` — скан всех конверсаций, отправка, статистика `{eligible, sent, failed, skipped_web}`. `preview()` — dry run.
- `bot/app/memory.py`: новое поле `nudge_sent: bool = False` в `Conversation` (однократность).
- `bot/app/config.py` + `bot/.env.example`: `NUDGE_ENABLED` (bool, true), `NUDGE_DELAY_HOURS` (36), `NUDGE_MAX_AGE_HOURS` (336 = 14д), `NUDGE_HOUR` (11), `NUDGE_MINUTE` (0), `NUDGE_PROXY_URL`.
- `bot/app/scheduler.py`: фоновая задача `_nudge_loop()` (ежедневно в 11:00 МСК); после рассылки уведомляет админов в MAX. `start()` → `list[Task]` (дайджест + nudge).
- `bot/app/main.py`: `GET /admin/nudge/preview` (dry run), `POST /admin/nudge/send` (ручной запуск); `nudge_enabled` в `/health`.
- `bot/tests/test_nudge.py` (новый, 31 тест): is_nudgeable по всем этапам/условиям, compose_message с персонализацией, send_nudge MAX/Telegram/web, run_nudges, админ-эндпоинты.

**Как проверено:** `cd bot && python3 -m pytest -q` → 128 passed.
**Решения и нюансы:**
- Напоминание отправляется **ровно один раз** (`nudge_sent=True`) — без спама.
- Web-пользователи пропускаются (push невозможен).
- Проверка ежедневно в 11:00 МСК (утро удобнее для родителей).
- После отправки — админы получают сводку в MAX.
- Нулевой риск: не меняет лид-менеджер/CRM/LLM — только отправляет сообщение.
- Работает на ВСЕХ каналах: MAX и Telegram (через тот же SOCKS5-прокси).
**Деплой:** после мёрджа — редеплой прод `bot` из `main`. Опционально: настроить `NUDGE_DELAY_HOURS`, `NUDGE_HOUR` в прод `.env`.
**Осталось / следующий шаг:** п.№ 4 — сбор отзывов после первых занятий; п.№ 5 — голосовые сообщения.

---

### Сессия 18 (агент — Devin) — Обязательная регистрация перед доступом к боту — PR #100

**Дата:** 2026-07-06
**PR:** #100 — feat(bot): обязательная регистрация перед использованием бота (`devin/1783365583-registration-gate` → main)
**Запрос владельца:** «Чтобы пользоваться ботом — необходимо сначала зарегистрироваться! ФИО взрослого, имя и дата рождения ребёнка, номер телефона — после того как мы получаем лид и информацию о том кто пользуется ботом и какие запросы делает — открыть доступ к его возможностям!»

**Что сделано (только `bot/`):**
- `bot/app/registration.py` (новый): модуль обязательной регистрации. При первом контакте бот просит: ФИО родителя → имя ребёнка → дата рождения/возраст → телефон. После заполнения всех полей → лид уходит в BigBen CRM, доступ к боту открывается. `is_registered(conv)` — проверяет статус (с учётом флага `REGISTRATION_REQUIRED`). `start_registration(conv)` — начинает поток (welcome + первый вопрос, пропускает уже заполненные поля). `handle_registration_step(conv, text, bigben)` — обрабатывает каждый шаг. `_submit_registration(conv, bigben)` — отправляет лид в CRM.
- `bot/app/memory.py`: новый этап `STAGE_REGISTRATION`; новые поля `registered: bool`, `registration_step: str` в `Conversation`; добавлена десериализация.
- `bot/app/config.py` + `bot/.env.example`: `REGISTRATION_REQUIRED` (bool, true по умолчанию).
- `bot/app/ai_core.py`: `handle_message()` — gate: если не зарегистрирован → перенаправляет в `registration`; `handle_start()` — для новых пользователей начинает регистрацию вместо приветствия; сохраняет `registered` при `/start` повторного пользователя.
- `bot/app/main.py`: `registration_required` в `/health`.
- `bot/tests/conftest.py` (новый): `autouse` фикстура — отключает регистрацию для всех существующих тестов (чтобы не ломать 128 старых).
- `bot/tests/test_registration.py` (новый, 18 тестов): unit-тесты каждого шага + интеграционные (полный flow через `handle_message`/`handle_start`).

**Как проверено:** `cd bot && python3 -m pytest -q` → 146 passed (128 старых + 18 новых).
**Решения и нюансы:**
- Регистрация работает на ВСЕХ каналах одинаково (MAX, Telegram, виджет) — gate в общем `handle_message`.
- `REGISTRATION_REQUIRED=false` — отключает gate (для отладки/обратной совместимости).
- Данные сразу сохраняются как лид в CRM с source «MAX-бот Фоксинбург — регистрация».
- Если пользователь уже заполнил часть полей (напр. через предыдущие сессии) — шаг пропускается.
- Повторный `/start` НЕ сбрасывает регистрацию (`was_registered` сохраняется через reset).
- Nudge-модуль: незарегистрированные пользователи НЕ получают напоминания (этап `registration` не в `_NUDGE_ELIGIBLE_STAGES`).
**Деплой:** после мёрджа — редеплой прод `bot` из `main`. Добавить `REGISTRATION_REQUIRED=true` в прод `.env` (или оставить по умолчанию). Существующие пользователи с `registered=false` в state.json будут перенаправлены на регистрацию при следующем сообщении.
**Осталось / следующий шаг:** п.№ 4 — сбор отзывов после первых занятий; п.№ 5 — голосовые сообщения.

---

### Сессия 19 (агент — Devin) — Телефон и ссылка на чат в уведомлениях админу — PR #101

**Дата:** 2026-07-06
**PR:** #101 — fix(bot): телефон клиента и ссылка на чат в уведомлениях админу (`devin/1783366043-admin-phone-link` → main)
**Запрос владельца:** «Хочу чтобы в заявках или когда просят администратора — приходило не ID клиента, а номер телефона клиента и ссылка на чат с ним, чтобы можно было сразу ему написать.»

**Что сделано (только `bot/`):**
- `bot/app/admin_router.py`: новая функция `_client_contact_block(conv)` — формирует блок контактной информации: 👤 имя родителя, 📱 телефон, 💬 ссылка на чат. Ссылка зависит от канала: MAX → `https://max.ru/chat/{user_id}`, Telegram → `tg://user?id={chat_id}`, виджет → «ответить по телефону». Если телефона нет — fallback на raw user_id. Функция `hand_off()` теперь использует этот блок вместо `Клиент MAX id: {user_id}`.
- `bot/tests/test_admin_contact.py` (новый, 7 тестов): покрытие всех каналов (MAX/TG/web) с телефоном и без.

**Как проверено:** `cd bot && python3 -m pytest -q` → 153 passed.
**Решения и нюансы:**
- Формат уведомления: имя + телефон + кликабельная ссылка на чат. Админ может сразу написать клиенту.
- Работает на всех каналах: MAX (deep link на чат), Telegram (`tg://user?id=`), виджет (телефон).
- Если данные ещё не собраны (напр. handoff до регистрации) — показывает user_id как fallback.
**Деплой:** после мёрджа — редеплой прод `bot` из `main`. Без доп. настроек.
**Осталось / следующий шаг:** п.№ 5 — сбор отзывов; п.№ 6 — голосовые сообщения.

### Сессия 20 (агент — Devin) — Скрыть кнопки до регистрации + эндпоинт сброса памяти — PR #102

**Дата:** 2026-07-06
**PR:** #102 — feat(bot): скрыть кнопки до завершения регистрации + эндпоинт сброса памяти (`devin/1783367017-hide-buttons-reg` → main)
**Запрос владельца:** «Нужно скрыть доп кнопки под сообщениями до окончания регистрации! После того как регистрация прошла — можно предложить все меню и начать общение!»

**Что сделано (только `bot/`):**
- `bot/app/main.py`:
  - Новая функция `_user_registered(user_id)` — проверяет завершена ли регистрация (через `is_registered(conv)`).
  - **MAX:** `bot_started` и `/start` — `_main_menu()` показывается только после регистрации; `/menu` — если не зарегистрирован, направляет в registration flow; все сообщения до регистрации — без кнопок и без спец-интентов (homework, handoff); `message_callback` — если не зарегистрирован, кнопки игнорируются (ответ из registration flow).
  - **Telegram:** после `/start` — если не зарегистрирован, все сообщения обрабатываются без кнопок.
  - **Виджет:** если не зарегистрирован — `buttons: []` в ответе.
  - `POST /admin/users/{user_id}/reset` — новый эндпоинт для сброса памяти пользователя (валидирует ADMIN_TOKEN, проверяет существование, вызывает `store.reset()`).

**Как проверено:** `cd bot && python3 -m pytest -q` → 146 passed.
**Решения и нюансы:**
- Во время регистрации бот не реагирует на интенты HOMEWORK / HANDOFF — всё направляется в registration flow. Это предотвращает «побег» из регистрации через кнопки.
- После завершения регистрации автоматически показывается полное меню — пользователь сразу видит все возможности.
- Эндпоинт `/admin/users/{user_id}/reset` позволяет владельцу сбрасывать любого пользователя для тестирования (`curl -X POST -H "X-Admin-Token: ..." .../admin/users/{id}/reset`).
**Деплой:** задеплоен на прод сразу после мёрджа. Nginx перенастроен на новый контейнер `bot-bot-1`. TG polling работает. Пользователь 5897639 (+79996960441) сброшен через API.
**Операционная работа в этой сессии:**
- SSH на VPS: `git pull origin main` + `docker compose build --no-cache` + `docker compose up -d`.
- Nginx: `foxinburg-frontend` проксирует `bot.dymova-english.ru` → `bot-bot-1:8000` (контейнер подключен к сети `foxinburg_foxinburg`). Старый `foxinburg-maxbot` остановлен.
- TG polling восстановлен (бот перестал отвечать из-за устаревшей версии на проде).
- Пользователь 5897639 сброшен для тестирования регистрации.
**Осталось / следующий шаг:** п.№ 5 — сбор отзывов; п.№ 6 — голосовые сообщения.

### Сессия 21 (агент — Devin) — Обновление страницы дошкольников — PR #104

**Дата:** 2026-07-06
**PR:** #104 — fix(site): обновление страницы дошкольников — возраст 2-6, цены, расписание (`devin/1783367943-doshkolniki-update` → main)
**Запрос владельца:** обновить страницу дошкольников: возраст 2–6 лет, «записаться на пробный урок», мини-группы до 7 детей, вечерние после сада, 45 мин (2–3 года) / 60 мин (4–6), стоимость 6 550 / 9 000 ₽, пробный 820 / 1 125 ₽, авторская программа Дымовой В.А., диагностику → пробное занятие.

**Что сделано:**
- `prototype/page_doshkolniki.html`: все правки по запросу — цены, возрасты, расписание, CTA.
- Tilda (pageid 151228606): обновлён HTML-блок T123, опубликовано.

**Как проверено:** визуальная проверка на live-странице https://dymova-english.ru/doshkolniki.
**Деплой:** опубликовано через Tilda.
**Осталось / следующий шаг:** обновить карточки дорожных карт на сайте.

### Сессия 22 (агент — Devin) — Обновление дорожных карт + 3 новых дошкольных — PR #105

**Дата:** 2026-07-06
**PR:** #105 — assets(roadmaps): обновлённые карточки + 3 новых дошкольных (baby, 3-5, 5-6) (`devin/1783435335-roadmap-cards-update` → main)
**Запрос владельца:** «Замени этими карточками карточки на сайте! И тут добавились ещё новые» + «для этих карточек можно новый фон взять — не такой цвет как уже есть»

**Что сделано:**
- Конвертировано 13 HTML-карточек из архива `roadmap_cards.zip` в PNG (1200×1680, Playwright + Chromium).
- Извлечены данные из 3 docx-файлов (Baby_course.docx, -2.docx, docx) — понедельные программы агрегированы в помесячные темы.
- Созданы 3 новых HTML-карточки дошкольников с уникальными фонами:
  - `baby-course.html/png` — 2–3 года, Starter, фиолетовый фон (#1a0a2e), розовый акцент (#f472b6).
  - `preschool-3-5.html/png` — 3–5 лет, Starter, тёмно-зелёный фон (#0a2818), изумрудный акцент (#34d399).
  - `preschool-5-6.html/png` — 5–6 лет, Pre-A1, янтарный фон (#1c1007), оранжевый акцент (#fb923c).
- Все 16 PNG + 17 HTML-исходников коммитнуты в `brand-assets/roadmaps/` (PNG) и `brand-assets/roadmaps/src/` (HTML).
- **Tilda — 3 страницы обновлены и опубликованы:**
  - `/mladshie-shkolniki`: заменён commit-hash → `main` в 18 URL карточек (My Level 1–4, Super Minds 1–4, Весёлый урок).
  - `/podrostki`: заменён commit-hash → `main`, переименовано A1 → A1+, добавлены 3 новых уровня (A2+, B1, B1+).
  - `/doshkolniki`: добавлена секция «Лестница знаний» с 3 карточками (Baby Course, 3–5 лет, 5–6 лет).

**Как проверено:** визуальная проверка PNG-файлов (все 16 отрендерены корректно, фоны отличаются). Tilda — страницы опубликованы.
**Решения и нюансы:**
- URL картинок теперь ссылаются на ветку `main` (не на commit-hash), что позволяет обновлять без правки Tilda.
- Новые дошкольные карточки используют уникальные цветовые схемы, отличающиеся от существующих (синих) карт.
- Get Involved A1 переименован в A1+ (по данным из новых HTML-шаблонов).
**Деплой:** все 3 Tilda-страницы опубликованы. Картинки доступны через raw.githubusercontent.com/main/.
**Осталось / следующий шаг:** п.№ 7 — сбор отзывов; п.№ 8 — голосовые сообщения.

### Сессия 23 (агент — Devin) — Ссылка ЛК + маршруты адресов на Яндекс Картах — PR #107

**Дата:** 2026-07-06
**PR:** #107 — fix(site): ссылка ЛК → members/login + маршруты адресов на Картах (`devin/1783444211-fix-lk-link-and-address-maps` → main)
**Запрос владельца:** «Замени ссылку для входа в ЛК на https://dymova-english.ru/members/login, а также проверь, что при нажатии на адрес — маршрут строится правильно! В шапке при нажатии на Ракетостроителей 9к3 маршрут строится на Лихачевский 76к1 почему-то!»

**Что сделано:**
- **ЛК-ссылка:** на всех 19 Tilda-страницах заменена `href="/lmsfoxinburg"` → `href="https://dymova-english.ru/members/login"` (в шапке и подвале).
- **Маршруты адресов:** на всех 19 страницах исправлены ссылки Яндекс Карт в шапке:
  - Лихачевский 76к1 → `maps/org/foxinburg/162408588499/` (было 112008441352 — перепутано).
  - Ракетостроителей 9к3 → `maps/org/foxinburg/112008441352/` (было 162408588499 — перепутано).
- **Главная страница:** отдельная правка — header-блок `rec2421794631` (не содержит `fxb-shapka`).
- **Прототип:** `tilda_dropdown_menu.html` — обновлён `href` кнопки ЛК.

**Как проверено:** curl-проверка 5 ключевых страниц (doshkolniki, mladshie-shkolniki, podrostki, main, kontakty): все `fxb-addr` ссылки ведут на правильные org ID.
**Решения и нюансы:**
- Org ID 162408588499 — филиал Лихачевский 76к1; org ID 112008441352 — филиал Ракетостроителей 9к3 (проверено через Яндекс Карты API).
- Прототип `tilda_topbar.html` использует адресную маршрутизацию (`rtext=~address`), а не org-page URL — там замена не требовалась.
**Деплой:** все 19 Tilda-страниц опубликованы через `tp__fetch` API.
**Осталось / следующий шаг:** п.№ 9 — сбор отзывов; п.№ 10 — голосовые сообщения.

---

## Текущий статус / Где остановились

**Последний влитый PR:** **#115** (BigBen lead defaults). В работе: PR «не выдумываем + перевод на администратора + журнал пробелов» (ветка `devin/1783540723-foxi-no-hallucination`) — после мержа переразвернуть прод из `main`.

**Telegram:** @foxinburg_bot работает через long-polling + SOCKS5-прокси (РКН блокирует и inbound, и outbound к api.telegram.org). `TELEGRAM_POLLING=true`, `TELEGRAM_PROXY_URL` в прод `.env`.

**Прод:** бот «Фоксинбург» на ВМ Yandex Cloud (`yc-user@89.169.132.104`), из `main` (commit 2842ed5). LLM: ProxyAPI (gpt-4o-mini) + OpenRouter fallback. Вебхук MAX `https://bot.dymova-english.ru/webhook` активен. Чат-виджет на сайте (`foxi.js`). SSH-ключ `~/.ssh/foxinburg_vps` (секрет `VPS_SSH_PRIVATE_KEY_OWNER`). Nginx `foxinburg-frontend` проксирует bot.dymova-english.ru → `bot-bot-1:8000`.

**Каналы бота (все работают на одном ядре):**
- MAX — webhook (`/webhook`)
- Telegram — long-polling через прокси
- Виджет на сайте — `/api/chat` + `foxi.js`

**Незакрытые хвосты:**
- Следить за сроком платного SOCKS5-прокси (истечёт — Telegram замолчит).
- При пересоздании контейнера `bot-bot-1` — заново подключить к сети `foxinburg_foxinburg` (`docker network connect foxinburg_foxinburg bot-bot-1`).

**План фич от владельца (по приоритету, из переписки):**
1. ✅ №12 — чат-виджет на сайте (PR #94).
2. ✅ №13 — Telegram (PR #95, #96, #98).
3. ✅ Возврат «недозаявок» — тёплые напоминания (PR #99).
4. ✅ Обязательная регистрация перед доступом к боту (PR #100).
5. ✅ Телефон + ссылка на чат в уведомлениях админу (PR #101).
6. ✅ Скрыть кнопки до регистрации + reset endpoint (PR #102).
7. ✅ Обновление страницы дошкольников — возраст 2–6, цены, расписание (PR #104).
8. ✅ Обновление дорожных карт + 3 новых дошкольных карточки (PR #105).
9. ✅ Ссылка ЛК → members/login + исправление маршрутов адресов на Яндекс Картах (PR #107).
10. Сбор отзывов после первых занятий (довольных — на Яндекс/2ГИС, недовольных — админу в личку).
11. Голосовые сообщения (распознавание голоса клиента).

> **Журналирование:** перед каждым PR добавляй сюда подробную запись по шаблону из `.agents/skills/session-journal/SKILL.md` (skill `session-journal`). Это требование владельца — журнал ведётся по каждому PR автоматически.

---

## Технические заметки

### Как вставить кастомный блок в Tilda
1. В редакторе страницы нажать «+» (добавить блок)
2. Выбрать «Другое» -> «HTML-код» (блок T123)
3. Вставить содержимое файла `tilda_*.html` целиком (HTML + CSS + JS)
4. Сохранить и опубликовать

### Как подключить форму заявки через нативный попап Tilda
1. Добавить блок формы с popup (категория «Форма» в библиотеке)
2. В настройках формы убедиться, что используются настройки проекта (не кастомные)
3. Добавить скрытое поле `source` со значением, указывающим блок/кнопку
4. Кнопку в кастомном HTML привязать к попапу через `href="#popup:имя"`

### Структура верха страницы (порядок сверху вниз — обновлено в Сессии 5)
1. Выпадающее меню (fxb-menu) — кастомное, скрывает нативную шапку Tilda через JS
2. Верхняя панель (fxb-topbar) — бейдж + адреса + телефоны + часы работы + Max Bot
3. Узкая шапка «Летние программы» (fxb-summer)
4. Слоган «Английский не для школы, а для жизни!» (кастомный, скрывает нативный через JS)
5. Первый экран (hero) — основной баннер

### Контакты для Max-ссылок
- Общий бот: https://max.ru/id611904726658_bot
- Бизнес-канал: https://max.ru/id611904726658_biz
- Для кнопки «Написать в Max» рядом с телефоном — используем ссылку на бот как fallback

---

### Сессия 6 — Полная переработка дизайна по отзыву владельца

**Дата:** 27 июня 2026

#### Замечания владельца:
1. «Не нравится доп полоска с часами работы — объединить с шапкой лаконично»
2. «Метка Хорошее место обрезано»
3. «Весь верх не должен быть в полосочку — одна высокая шапка»
4. «Слоган — формат оформления не нравится, исключить обычные эмодзи»
5. «Использовать картинки или иконки подходящие по смыслу и дизайну»
6. «Блок про диагностику → запись на новый год, открыта запись, успей забронировать»
7. «Сведения об образовательной организации — некрасиво, не выровнено, сокращено — вернуть полное»
8. «Использовать брендированные элементы и логотипы на заднем плане»
9. «После Наши преимущества — запись на новый учебный год с ценой и описанием»
10. «Отзывы родителей — письменные и видео»
11. «FAQ — ответы на частые вопросы»
12. «Контакты с картой где отмечены филиалы + ярлычки соцсетей/мессенджеров»
13. «Убрать кнопку связаться с руководителем»
14. «Все адреса и номера телефонов кликабельные»
15. «При нажатии на адрес — вести к точке организации Языковая школа Фоксинбург»
16. «Подвал с полным меню, политикой конфиденциальности, информацией об ИП»
17. «Весь сайт в одном брендовом стиле!»

#### Брендовые элементы получены:
- Декоративные жёлтые завитки (decor-swirl-yellow-1.png, decor-swirl-yellow-2.png)
- Жёлтый blob (decor-blob-yellow.png)
- Жёлтый зигзаг (decor-zigzag-yellow.png)
- Маленькие волны (decor-wave-small-1.png, decor-wave-small-2.png)
- Логотип-печать тёмный (logo-stamp-dark.png/.jpg)
- Голова лисы жёлтая/светлая (fox-head-yellow.png)
- Голова лисы фиолетовая/тёмная (fox-head-purple.png)
- Брендбук PDF (brand-guide.pdf)

#### Что сделано:

1. **Единая шапка** (`tilda_header_unified.html`) — полная переработка:
   - Два ряда в одном блоке: инфо-полоса (адреса + телефоны + часы + Max Bot) + навигация (лого + бейдж + меню + кнопки)
   - Все адреса кликабельные → ведут на точку организации в Яндекс.Картах
   - Все телефоны кликабельные (tel:)
   - Выпадающие подменю «Наши курсы» и «Наши направления» с SVG иконками
   - Убрана кнопка «связаться с руководителем»
   - Мобильный бургер-меню
   - JS скрывает нативную шапку Tilda

2. **Слоган** (`tilda_slogan.html`) — переделан:
   - Убраны emoji, вместо 🎓 используется брендовая картинка fox-head-yellow.png
   - Декоративные элементы (swirl, zigzag) на фоне с opacity 8%
   - Градиент #392852 → #4a2b6e → #662d92

3. **CTA Запись** (`tilda_cta_enrollment.html`) — новый блок:
   - Заменяет старый «Записаться на диагностику»
   - Тег «Набор открыт», заголовок «Запись на новый учебный год 2026/27»
   - «Успейте забронировать место в группе с удобным расписанием!»
   - Форма: имя, ребёнок, телефон, формат (офлайн Лихачевский/Ракетостроителей/онлайн)
   - 3 feature-пункта с SVG иконками
   - Декоративные элементы на фоне

4. **Летние программы** (`tilda_summerbar.html`) — убраны emoji:
   - Вместо ☀️ используется SVG иконка солнца
   - Правильные URL-ы страниц

5. **Тарифы** (`tilda_pricing_enrollment.html`) — новый блок после «Наши преимущества»:
   - 3 карточки: Офлайн мини-группы (от 5 600₽/мес), Онлайн занятия (от 4 800₽/мес), Пробное (бесплатно)
   - Бейдж «Популярное» на онлайн карточке
   - Описание формата и расписания

6. **FAQ** (`tilda_faq.html`) — новый блок:
   - 9 вопросов-ответов из оригинального сайта
   - Аккордеон с анимацией

7. **Контакты с картой** (`tilda_contacts_map.html`) — новый блок:
   - Яндекс.Карта с двумя метками филиалов
   - Два блока филиалов с кликабельными адресами/телефонами
   - Ссылки на соцсети: VK, Max, Telegram, WhatsApp

8. **Подвал** (`tilda_footer.html`) — новый блок:
   - 4 колонки: бренд + навигация + о школе + контакты
   - Соцсети: VK, Max, Telegram, WhatsApp
   - ИП Дымова Вероника Александровна, ИНН 611904726658
   - Политика конфиденциальности, Публичная оферта
   - Декоративный элемент fox-head-yellow на фоне

9. **Сведения** (`tilda_svedeniya.html`) — переработан:
   - CSS Grid для выравнивания (260px : 1fr)
   - Полное содержание: 19 пунктов (все разделы из оригинала)
   - Все ссылки кликабельные

10. **Брендовые элементы сохранены** в `brand-assets/` (11 файлов)

#### Порядок блоков в main_combined_v3.html (133K, 1729 строк):
1. tilda_header_unified.html (единая шапка)
2. tilda_summerbar.html (летние программы)
3. tilda_slogan.html (слоган с лисой)
4. tilda_cta_enrollment.html (запись на новый год)
5. tilda_onboarding.html (как начинается обучение)
6. tilda_photobank_head.html (заголовок фотобанка)
7. tilda_photobank_gallery.html (галерея)
8. tilda_directions.html (направления)
9. tilda_team.html (команда)
10. tilda_languages.html (другие языки)
11. tilda_advantages.html (преимущества)
12. tilda_pricing_enrollment.html (тарифы + запись)
13. tilda_faq.html (FAQ)
14. tilda_svedeniya.html (сведения)
15. tilda_contacts_map.html (контакты + карта)
16. tilda_footer.html (подвал)

---

### Сессия 7 (Devin — агент-продолжение)

**Дата:** 18 июня 2026

#### Задача от владельца:
Критическая обратная связь: шапка сайта выглядит как «куча полос» — нужно объединить ВСЕ элементы верхней части в ОДИН единый профессиональный блок с единым стилем. Дословно: «Мне не нравится что ты тут налепил! Сделай все это место в одном едином стиле! красиво по профессиональному!!! ну что это хрень?!»

#### Что сделано:

1. **Полностью переписана единая шапка** (`tilda_header_unified.html` v4):
   - ОДИН контейнер `#fxb-hero` с единым градиентом: `linear-gradient(170deg, #241a36 0%, #392852 30%, #662d92 70%, #7a3db0 100%)`
   - 4 секции объединены внутри одного фона (без отдельных цветных полос):
     1. **Инфо-строка** — адреса (2 филиала, ведут на Яндекс.Карты org/foxinburg/162408588499), телефоны (кликабельные tel:), часы работы, Max Bot
     2. **Навигация** — лого Фоксинбург + бейдж «Хорошее место 5.0» (iframe) + выпадающие меню «Наши курсы» и «Наши направления» + Новости + Вакансии + Контакты + кнопка «Max канал» + кнопка «Вход в ЛК»
     3. **Летние программы** — SVG иконка солнца + 6 ссылок на программы (subtle `rgba(238,115,73,.15)` фон)
     4. **Слоган** — «Английский не для школы, а для жизни!» с брендовой лисой fox-head-yellow.png
   - Секции разделены тонкими полупрозрачными бордюрами `1px solid rgba(255,255,255,.08)` вместо жёстких цветовых разделов
   - Декоративные элементы (decor-swirl-yellow-1.png, decor-zigzag-yellow.png) как фоновые наложения с `opacity: .04`
   - Мобильный бургер-меню на `@media(max-width:960px)`

2. **CSS-скрытие нативных блоков Tilda** — добавлено прямо в `<style>`:
   - `.r.t-rec{display:none!important}` — скрывает ВСЕ нативные блоки
   - `.r.t-rec:has(#fxb-hero){display:block!important}` — показывает только наш T123 блок
   - Fallback: перечислены все известные record ID для скрытия (для браузеров без поддержки `:has()`)
   - Это полностью убрало нативную шапку Tilda (t770 блок rec2421715951) и все остальные нативные блоки

3. **Создан main_combined_v4.html** (128K) — объединённый файл со всеми 16 блоками:
   - Новая шапка v4 (включает инфо + навигацию + летние программы + слоган)
   - + все остальные блоки из v3 (CTA, онбординг, фотобанк, направления, команда, языки, преимущества, тарифы, FAQ, сведения, контакты, подвал)

4. **Загружено в Tilda** и опубликовано:
   - Метод: `tp__saveOnlyOneFieldInRecord('2421794631', 'code', 'code', htmlContent)` через Playwright CDP
   - Публикация через кнопку «Опубликовать» в редакторе Tilda
   - Рабочая копия доступна: https://dymova-english.ru/index-new

#### Технические заметки:

- **Почему JS не скрывал нативную шапку:** Tilda использует блок типа `t770` для меню (ID `rec2421715951`). Предыдущие селекторы (`.t228,.t188,.t210` и т.д.) не покрывали этот тип. Кроме того, JS внутри T123 блоков в Tilda может выполняться с задержкой или не выполняться вообще при загрузке страницы. **Решение:** CSS `!important` правила работают надежнее, т.к. применяются парсером до рендеринга.
- **`:has()` CSS selector** — поддерживается Chrome 105+, Safari 15.4+, Firefox 121+. Для fallback добавлены конкретные record ID.
- **Порядок загрузки в Tilda:** Сохранение через `tp__saveOnlyOneFieldInRecord` → POST `/page/submit/` с параметрами `comm=saverecord, pageid=151210576, recordid=2421794631, onlythisfield=code, code=<HTML>`

#### Оставшиеся задачи (для следующего агента):

1. **Блок «Отзывы родителей»** — письменные + видео отзывы (ещё не создан)
2. **Перенос на основную страницу** — после утверждения владельцем, перенести всё с /index-new (pageid=151210576) на основную страницу (pageid=32889798)
3. **Подключение форм** — формы в T123 блоках НЕ подключены к нативным формам Tilda (Google Sheets, Webhook, CRM и т.д.). Нужно либо подключить через Tilda API, либо добавить JS-обработчики для отправки данных
4. **Фотобанк** — заглушки «Добавьте фото» — нужно загрузить реальные фото школы
5. **Команда** — нет фотографий и видео педагогов
6. **Проверка мобильной версии** — полная проверка всех блоков на мобильных устройствах

#### Ключевые значения для продолжения:
- **Tilda Project ID:** 2053071
- **Page ID (рабочая копия):** 151210576, slug: `/index-new`
- **Page ID (основная):** 32889798
- **T123 Record ID:** 2421794631
- **Tilda аккаунт:** vkrivobokova4@gmail.com (пароль через секрет `TILDA_PASSWORD`)
- **Yandex Org ID (Лихачевский):** 162408588499
- **GitHub branch:** `devin/1782590824-session6-redesign`
- **PR:** https://github.com/Dymovgrigory/Dymova-english/pull/5
- **Дизайн-система:** Purple #392852, Purple-2 #662d92, Orange #ee7349, Yellow #fcf951, Ink #1e1433, шрифт Montserrat
- **GitHub brand assets:** `https://raw.githubusercontent.com/Dymovgrigory/Dymova-english/devin/1782590824-session6-redesign/brand-assets/`

---

### Сессия 8 — Бесшовный верх + исправление символов на карточках

**Дата:** 18 июня 2026 (продолжение, новый агент Devin)

#### Замечания владельца:
1. «Весь верх — включая слоган — нужно объединить в 1 стильный блок, чтобы не было видно что сделано на Тильде — как будто полностью кастомный сайт»
2. «На некоторых карточках появились какие-то символы» (emoji-коды рендерились некорректно)

#### Что сделано:

1. **Единая шапка v5** (`tilda_header_unified.html`) — полная переработка:
   - Убраны все `border-bottom` разделители между секциями (инфо, навигация, летние, слоган) — теперь ОДИН бесшовный блок
   - Единый контейнер `#fxb-hero` → внутри `.fxb-w` с 4 секциями без визуальных границ
   - Новый градиент: `linear-gradient(165deg, #1e1433 0%, #392852 25%, #662d92 60%, #7a3db0 88%, #8f52c4 100%)`
   - Добавлен 3-й декоративный элемент (fox-head-yellow) на фоне с opacity .025
   - Анимация плавающей лисы в слогане (`fxbFoxFloat`)
   - Добавлен подзаголовок «Языковая школа в Долгопрудном для детей от 3 до 16 лет»
   - Летние программы — стиль тегов с pill-border вместо отдельной полосы
   - Контактная строка — компактная, по центру, без фона-подложки
   - Dropdown анимация (`fxbddIn`)
   - SVG иконка часов и мессенджера Max в навигации

2. **Исправлены emoji-символы на карточках:**
   - `tilda_directions.html`: заменены `&#x1f9f8;` (мишка), `&#x1f392;` (рюкзак), `&#x1f680;` (ракета) на кастомные SVG-иконки (смайлик, рюкзак, ракета) в фирменных цветах
   - `tilda_team.html`: заменены `&#x1f98a;` (лиса emoji) на `fox-head-yellow.png` (брендовая лиса); `&#x25b6;` (треугольник) на SVG play-button
   - `tilda_languages.html`: заменены `&#x1f1e9;&#x1f1ea;` (флаг DE), `&#x1f1e8;&#x1f1f3;` (флаг CN) на текстовые бейджи «DE»/«CN» с pill-стилем; `&#x1f98a;` на `fox-head-yellow.png`

3. **Создан main_combined_v5.html** (133K, 1724 строк) — объединённый файл 16 блоков с новой шапкой v5 и исправленными карточками

#### Порядок блоков в main_combined_v5.html:
1. `tilda_header_unified.html` — единая бесшовная шапка v5
2. `tilda_cta_enrollment.html` — запись на новый учебный год
3. `tilda_onboarding.html` — как начинается обучение
4. `tilda_photobank_head.html` — заголовок фотобанка
5. `tilda_photobank_gallery.html` — галерея
6. `tilda_directions.html` — направления (SVG иконки)
7. `tilda_team.html` — команда (брендовая лиса)
8. `tilda_languages.html` — другие языки (текстовые флаги)
9. `tilda_advantages.html` — преимущества
10. `tilda_pricing_enrollment.html` — тарифы + запись
11. `tilda_faq.html` — FAQ
12. `tilda_svedeniya.html` — сведения
13. `tilda_contacts_map.html` — контакты + карта
14. `tilda_footer.html` — подвал

---

### Сессия 9 — Разделение кода на отдельные T123 блоки в Tilda

**Дата:** 18 июня 2026 (продолжение)

#### Замечание владельца:
«В Тильда должно тоже код разделён на отдельные части! Для удобства!»

#### Что сделано:

1. **Разделение 2 больших T123 блоков → 14 отдельных T123**
   - Было: 2 блока по ~58K и ~62K символов (слиты из-за лимита 116K на блок)
   - Стало: 14 отдельных T123 блоков, каждый содержит код одной секции сайта
   - Каждый блок минифицирован (убраны комментарии, пробелы, CSS/JS сжаты)

2. **Минифицированные файлы** в `prototype/tilda_blocks_min/`:
   | # | Файл | Размер | Секция |
   |---|------|--------|--------|
   | 1 | tilda_header_unified_min.html | 14.8K | Шапка (бесшовная v5) |
   | 2 | tilda_cta_diagnostika_min.html | 11.2K | CTA диагностика |
   | 3 | tilda_onboarding_min.html | 10.1K | Онбординг |
   | 4 | tilda_photobank_gallery_min.html | 6.4K | Фотобанк |
   | 5 | tilda_directions_min.html | 7.0K | Направления |
   | 6 | tilda_team_min.html | 9.4K | Команда |
   | 7 | tilda_languages_min.html | 15.6K | Языки |
   | 8 | tilda_advantages_min.html | 5.6K | Преимущества |
   | 9 | tilda_pricing_enrollment_min.html | 6.0K | Тарифы |
   | 10 | tilda_faq_min.html | 7.4K | FAQ |
   | 11 | tilda_svedeniya_min.html | 6.4K | Сведения |
   | 12 | tilda_contacts_map_min.html | 8.4K | Контакты+Карта |
   | 13 | tilda_cta_enrollment_min.html | 7.1K | CTA запись на год |
   | 14 | tilda_footer_min.html | 6.7K | Подвал |

3. **Автоматизация загрузки** (`prototype/tilda_upload_blocks.py`):
   - Создание T123 блоков через Tilda API: `POST /page/submit/` с `comm=addnewrecord`, `tplid=131`
   - Сохранение кода: `POST /page/submit/` с `comm=saverecord`, `onlythisfield=code`
   - Сортировка: `POST /page/submit/` с `comm=saverecordssort`
   - Скрипт использует Playwright CDP для доступа к Tilda API через авторизованную сессию

4. **Record IDs T123 блоков на странице** (pageid 151210576):
   | # | Record ID | Секция |
   |---|-----------|--------|
   | 1 | 2421794631 | Шапка |
   | 2 | 2422504491 | CTA диагностика |
   | 3 | 2422576651 | Онбординг |
   | 4 | 2422576671 | Фотобанк |
   | 5 | 2422570311 | Направления |
   | 6 | 2422572491 | Команда |
   | 7 | 2422572511 | Языки |
   | 8 | 2422572521 | Преимущества |
   | 9 | 2422572531 | Тарифы |
   | 10 | 2422572561 | FAQ |
   | 11 | 2422572571 | Сведения |
   | 12 | 2422572611 | Контакты+Карта |
   | 13 | 2422572641 | CTA запись на год |
   | 14 | 2422572651 | Подвал |

#### Технические заметки:
- Tilda API: поле `code` для HTML-кода в T123 блоках (не `html`)
- Скрытые (off='y') блоки не принимают контент через API — нужно создавать новые
- Публикация через UI кнопку «Опубликовать» (API `comm=publishpage` не работает)
- Все 14 блоков успешно отображаются на https://dymova-english.ru/index-new

---

### Сессия 10 — Единый стандарт порядка блоков + новый блок «Отзывы»

**Дата:** 28 июня 2026

#### Запрос владельца (зафиксирован дословно):
Настроить порядок блоков на главной по единому стандарту. Блок записи на новый
учебный год был в самом низу — поднять наверх (сразу после шапки). Добавить блок
отзывов (письменные + видео). **«Эта структура должна быть неизменна, пока я сам
не скажу что-то поменять в очереди блоков!»**

#### Зафиксированный порядок блоков (НЕ МЕНЯТЬ без явного указания владельца):
| # | Секция | Record ID | fxb-id |
|---|--------|-----------|--------|
| 1 | Шапка / меню | 2421794631 | fxb-hero |
| 2 | Запись на новый учебный год (CTA) | 2422572641 | fxb-enrollment |
| 3 | Наши преимущества | 2422572521 | fxb-adv |
| 4 | Наши направления | 2422570311 | fxb-dir |
| 5 | Как начинается обучение | 2422576651 | fxb-onboarding |
| 6 | Команда | 2422572491 | fxb-team |
| 7 | Другие языки | 2422572511 | fxb-lang |
| 8 | Фотобанк филиалов | 2422576671 | fxb-gallery |
| 9 | Запись — 3 карточки (тарифы) | 2422572531 | fxb-pricing |
| 10 | **Отзывы (письменные + видео)** | **2422719781** | **fxb-reviews** |
| 11 | FAQ | 2422572561 | fxb-faq |
| 12 | Сведения об образовательной организации | 2422572571 | fxb-sved |
| 13 | Контакты | 2422572611 | fxb-contacts |
| 14 | Подвал | 2422572651 | fxb-footer |

#### Что сделано:
1. **Новый блок «Отзывы»** (`prototype/tilda_reviews.html` + `tilda_blocks_min/tilda_reviews_min.html`, ~16K):
   - Вкладки «Письменные» / «Видеоотзывы»
   - 6 письменных отзывов (имя родителя + ребёнок/возраст, 5★), фирменный стиль (Montserrat, #392852/#662d92/#ee7349)
   - 3 видеокарточки-плейсхолдера с плеером и модалкой — ссылки добавляются в `data-video` (VK Видео / YouTube / RuTube / Яндекс.Диск)
   - Создан как новый T123 (recordid 2422719781)
2. **Удалён устаревший блок** «Записаться на диагностику» (`fxb-cta-top`, recordid 2422504491) — заменён блоком записи на год.
3. **Переупорядочены все T123** в зафиксированный порядок (см. таблицу).
4. **Собран** `prototype/main_combined_v7.html` — превью главной в новом порядке.
5. Опубликовано и проверено на https://dymova-english.ru/index-new (desktop).

#### Технические заметки (важно для будущих сессий):
- **Редактор Tilda открывается только с параметром `&edit=y`**: `https://tilda.ru/page/?pageid=151210576&projectid=2053071&edit=y`. Без него страница рендерится пустой.
- **API сортировки блоков**: `comm=saverecordssort` принимает порядок как **массив** `sorts[0]=<id>&sorts[1]=<id>&...`, а НЕ как CSV `sort=`. Параметр `sort=` молча игнорируется (сервер всё равно отвечает `OK`). Команда найдена перехватом запроса штатной стрелки перемещения блока в редакторе.
- После вызова `saverecordssort` нужно **перезагрузить редактор** (F5), чтобы он подхватил серверный порядок, затем публиковать — иначе кнопка «Опубликовать» зальёт старый in-memory порядок.
- Скрытие/показ блоков на странице: `.r.t-rec{display:none!important}` + `.r.t-rec:has([id^="fxb-"]){display:block!important}` — любой блок с id, начинающимся на `fxb-`, показывается автоматически (поэтому новый `fxb-reviews` отобразился без доп. настроек).
- `comm=deleterecord` (pageid+recordid) — рабочее удаление блока.
- Скрипты: `tilda_reorder.py` (переупорядочивание в зафиксированный порядок), `tilda_upload_blocks.py` (список блоков обновлён под новый стандарт).

#### Осталось / на будущее:
- Видео в блоке отзывов (ждём ссылки от владельца).
- Перенос новой структуры на основную страницу (pageid 32889798) — только по команде владельца.

---

### Сессия 11 — Доп. страницы в едином фирменном стиле

**Дата:** 28 июня 2026

#### Запрос владельца (дословно):
«Теперь я хочу чтобы все дополнительные страницы были оформлены по образцу главной,
но не делать копию а только то что относится к доп странице… и проверить чтобы они
открывались и работали! …фирменный стиль прослеживался не только в шапке и подвале,
но и во всех блоках аккуратно понемного!»

#### Что сделано:
1. **Единая шапка** `prototype/tilda_shapka.html` (~14.6K) — slim-версия меню/контактов
   без главного слогана (у каждой доп. страницы свой герой). Те же цвета и навигация,
   что и `tilda_header_unified`, бургер-меню на мобильных.
2. **Генератор контента** `prototype/build_subpages.py` — единый CSS + словарь из 24
   SVG-иконок (без emoji), скролл-ревил (IntersectionObserver), адаптивные сетки.
   Конфиг каждой страницы — это словарь (герой-градиент, eyebrow, features, факты,
   пособия, CTA). Генерирует 7 файлов `page_*.html`.
3. **Контент под каждую страницу** (а не копия главной):
   - Направления: дошкольники (игра, 45 мин), младшие (база, Family&Friends/Kid's Box),
     подростки (ОГЭ/ЕГЭ, Solutions/Prepare, 60 мин).
   - Лето: Летняя Академия (оранжево-жёлтый герой), Онлайн, Поддерживающие, Оффлайн.
   - `page_kontakty.html` — оставлен как есть (уже фирстиль: кликабельные адреса/тел,
     карта Яндекс, соцсети); добавлены общие шапка + подвал.
4. **Загрузка и публикация** через `prototype/tilda_upload_subpages.py`:
   на каждую из 8 страниц обеспечены 3 блока T123 в порядке **шапка → контент → подвал**
   (создано 16 новых блоков шапки/подвала, контент перезаписан, выставлена сортировка),
   опубликовано «Опубликовать все» (19/19).
5. **Проверка** на лайве (desktop + узкий экран): шапка/подвал, брендовый герой,
   SVG-иконки, факты, CTA с 2 кнопками (форма сайта + Max), кликабельные контакты,
   карта; на мобильном — 1 колонка и рабочее бургер-меню.

| Страница | pageid | alias | shapka rec | content rec | footer rec |
|---|---|---|---|---|---|
| Дошкольники | 151228606 | doshkolniki | 2422785091 | 2421956151 | 2422785101 |
| Младшие | 151228676 | mladshie-shkolniki | 2422785301 | 2421956591 | 2422785311 |
| Подростки | 151228746 | podrostki | 2422785781 | 2421957231 | 2422785821 |
| Летняя Академия | 151229566 | letnyaya-akademiya | 2422785951 | 2421958001 | 2422785971 |
| Онлайн | 151229606 | online-zanyatiya | 2422786051 | 2421959491 | 2422786091 |
| Поддерживающие | 151229676 | podderzhivayushchie-online | 2422786271 | 2421959851 | 2422786281 |
| Оффлайн | 151229756 | standartnye-offline | 2422786401 | 2421960301 | 2422786431 |
| Контакты | 151228006 | kontakty | 2422786561 | 2421942111 | 2422786721 |

#### Технические заметки:
- **Чтения кода блока через API нет** (`getrecord`/`getfieldvalue` → `Wrong command`),
  а редактор не рендерит код T123 инлайн. Роли блоков (шапка/контент/подвал) надёжно
  определяются по **опубликованному HTML** (curl), что делает аплоадер идемпотентным
  после первой публикации.
- Сайт даёт периодические **403** на прямой fetch — добавлены ретраи с бэкоффом.
- Создание блока: `comm=addnewrecord` + `tplid=131` → новый T123; запись кода:
  `comm=saverecord`, `onlythisfield=code`, `code=...`.

#### Осталось / на будущее:
- Рабочие копии легаси-страниц (reading/grammar/preparation/news/vacant/lmsfoxinburg)
  в новом стиле — по согласованию с владельцем (оригиналы не трогаем).
- Реальные фото пособий вместо плейсхолдеров на страницах направлений.

---

## Файловая структура репозитория

```
/
├── README.md                          # Описание репозитория
├── PROMPT_FOXINBURG.md                # Полный промт для агента
├── DEVLOG.md                          # Этот журнал разработки
├── brand-assets/                      # Брендовые элементы и логотипы
│   ├── fox-head-purple.png            # Голова лисы (фиолетовая)
│   ├── fox-head-yellow.png            # Голова лисы (жёлтая, для фонов)
│   ├── logo-stamp-dark.png            # Круглый логотип-печать (тёмный)
│   ├── logo-stamp-dark.jpg            # Круглый логотип-печать (jpg)
│   ├── decor-swirl-yellow-1.png       # Декоративный завиток 1
│   ├── decor-swirl-yellow-2.png       # Декоративный завиток 2
│   ├── decor-blob-yellow.png          # Декоративный blob
│   ├── decor-zigzag-yellow.png        # Декоративный зигзаг
│   ├── decor-wave-small-1.png         # Маленькая волна 1
│   ├── decor-wave-small-2.png         # Маленькая волна 2
│   └── brand-guide.pdf               # Брендбук (PDF)
└── prototype/                         # HTML-прототипы блоков и страниц
    ├── main_combined_v3.html          # [устарел] Объединённый файл v3 (133K)
    ├── main_combined_v4.html          # [устарел] Объединённый файл v4
    ├── main_combined_v5.html          # Объединённый файл v5 (133K, 16 блоков, бесшовная шапка, SVG иконки)
    ├── tilda_header_unified.html      # ЕДИНАЯ ШАПКА v5 (бесшовный блок без полосок)
    ├── tilda_summerbar.html           # Летние программы (SVG иконки, без emoji)
    ├── tilda_slogan.html              # Слоган (брендовая лиса, без emoji)
    ├── tilda_cta_enrollment.html      # CTA «Запись на новый учебный год»
    ├── tilda_onboarding.html          # «Как начинается обучение»
    ├── tilda_photobank_head.html      # Заголовок фотобанка
    ├── tilda_photobank_gallery.html   # Фотобанк — галерея
    ├── tilda_directions.html          # «Наши направления» (SVG иконки, без emoji)
    ├── tilda_team.html                # «Команда» (брендовая лиса, SVG play)
    ├── tilda_languages.html           # Немецкий/Китайский (текстовые флаги, без emoji)
    ├── tilda_advantages.html          # «Наши преимущества»
    ├── tilda_pricing_enrollment.html  # Тарифы + запись (после преимуществ)
    ├── tilda_faq.html                 # FAQ — 9 вопросов-ответов
    ├── tilda_svedeniya.html           # Сведения (19 пунктов, полное содержание)
    ├── tilda_contacts_map.html        # Контакты + Яндекс.Карта + соцсети
    ├── tilda_footer.html              # Подвал (меню, ИП, политика, соцсети)
    ├── tilda_cta_diagnostika.html     # CTA — запись на диагностику
    ├── tilda_dropdown_menu.html       # [устарел] отдельное меню
    ├── tilda_topbar.html              # [устарел] отдельная верхняя панель
    ├── tilda_upload_blocks.py         # Скрипт автоматической загрузки блоков в Tilda
    ├── tilda_blocks_min/              # Минифицированные блоки для загрузки в Tilda
    │   ├── tilda_header_unified_min.html
    │   ├── tilda_cta_diagnostika_min.html
    │   ├── tilda_onboarding_min.html
    │   ├── tilda_photobank_gallery_min.html
    │   ├── tilda_directions_min.html
    │   ├── tilda_team_min.html
    │   ├── tilda_languages_min.html
    │   ├── tilda_advantages_min.html
    │   ├── tilda_pricing_enrollment_min.html
    │   ├── tilda_faq_min.html
    │   ├── tilda_svedeniya_min.html
    │   ├── tilda_contacts_map_min.html
    │   ├── tilda_cta_enrollment_min.html
    │   ├── tilda_reviews_min.html       # Отзывы (письменные + видео) — сессия 10
    │   └── tilda_footer_min.html
    ├── page_kontakty.html             # Страница «Контакты»
    ├── page_doshkolniki.html          # Страница «Для дошкольников»
    ├── page_mladshie_shkolniki.html   # Страница «Для младших школьников»
    ├── page_podrostki.html            # Страница «Для подростков»
    ├── page_letnyaya_akademiya.html   # Летняя Академия
    ├── page_online_zanyatiya.html     # Онлайн занятия
    ├── page_podderzhivayushchie_online.html # Поддерживающие онлайн
    └── page_standartnye_offline.html  # Стандартные оффлайн
```

---

## Сессия 12 — обновление страницы «Младшие школьники»

Запрос владельца: на странице `/mladshie-shkolniki` блок «Пособия и материалы»
заменить на линейку учебников **My Level 1–4** (с сайта mylevelbook.com): компактное
описание каждого + обложка, без стоимости. Приписка: «Комплекты учебников
приобретаются у нас в школе отдельно». В факте про утренние занятия указать, что
есть дневные, вечерние и утренние группы — в т.ч. для тех, кто учится во вторую смену.

Сделано:
- Обложки `mylevel-1..4.png` скачаны с mylevelbook.com, сжаты до 480px и залиты в
  `brand-assets/` (ветка `devin/1782590824-session6-redesign` — общий CDN-источник).
- `build_subpages.py`: `book()` теперь поддерживает обложку-картинку
  (`.fxb-book-cover--img`); добавлен настраиваемый `books_note`.
- Конфиг `page_mladshie_shkolniki.html`: 4 карточки My Level 1–4 (возраст, уровень
  CEFR, что внутри), заголовок «Учебники My Level», примечание про покупку
  комплектов; факт «Утро, день, вечер» с пояснением про вторую смену.
- Загружено на Tilda (pageid 151228676, content rec2421956591), опубликовано.
  Проверено на лайве: https://dymova-english.ru/mladshie-shkolniki

---

## Сессия 13 — рабочие копии легаси-страниц (reading/grammar/preparation)

Запрос владельца: сделать рабочие копии страниц «Курс по чтению», «Курс по
грамматике» и «Подготовка к школе» в едином фирменном стиле, формате и порядке.
Оригиналы (`/reading` 137726126, `/grammar` 137739566, `/preparation` 130390566)
НЕ трогать — замена по согласованию.

Сделано:
- В `build_subpages.py` добавлены конфиги `page_reading.html`, `page_grammar.html`,
  `page_preparation.html` (герой, преимущества, факты, CTA с кнопками заявки и Max).
- Созданы 3 копии-страницы в Tilda и опубликованы на временных алиасах:
  - `/reading-new`  (pageid 151292376)
  - `/grammar-new`  (pageid 151292406)
  - `/preparation-new` (pageid 151292476)
- Каждая страница = 3 блока T123: shapka + content + footer (единый стиль).
  Лишний блок-плеер T115, скопированный из исходной страницы, удалён.
- Настройки страниц (заголовок/описание/alias) заданы скриптом `tilda_set_settings.py`
  (кириллица вводится через Playwright `fill`, не через клавиатуру).
- Проверено на лайве (desktop + mobile 375px): единые шапка/подвал, SVG-иконки,
  факты, CTA; мобильная вёрстка в 1 колонку с рабочим бургер-меню.

Скрипты сессии: `tilda_set_settings.py`, `tilda_publish_pages.py`,
`tilda_upload_copies.py`, `tilda_delete_record.py`.

Оригиналы не изменялись. После согласования владельца алиасы копий заменят
оригинальные `/reading`, `/grammar`, `/preparation`.

---

## Сессия 13b — курс-ссылки на копии главной (/index-new)

Запрос владельца: на копии главной заменить ссылки курсов на новые копии.

Сделано (`tilda_relink_index.py`):
- В видимых кастомных T123-блоках страницы 151210576 заменены ссылки:
  `/reading→/reading-new`, `/grammar→/grammar-new`, `/preparation→/preparation-new`.
  - Шапка (rec2421794631): меню «Наши курсы» + топ-бар «Летние программы».
  - Подвал (rec2422572651): блок «Навигация».
- Сырой код блока берётся дословно из опубликованного HTML между маркерами
  `nominify begin/end` (без искажений сущностей/lazyload), затем `saverecord`.
- Старые скрытые блоки T770/T228/T457 (display:none на desktop и mobile) не
  трогали — пользователю не видны.
- Опубликовано; проверено на лайве: меню/подвал ведут на `*-new`, шапка с JS
  (выпадающее меню) работает.

Оригиналы /reading, /grammar, /preparation по-прежнему не тронуты.

---

## Сессия 14 — блок «Наши преимущества» на /index-new: контент с оригинала + интерактив

Запрос владельца: взять инфо из раздела «Наши преимущества» оригинала dymova-english.ru
и добавить в рабочую копию; кнопки — «Посмотреть пример» (обратная связь),
«Посмотреть видеообзор» (мобильное приложение), переход к блоку «Команда»,
кнопка воспроизведения видео (мини-группы); карточек можно больше 4.

Сделано (`fxb_adv_v2.html`, `tilda_update_adv.py`, блок fxb-adv rec2422572521):
- Блок расширен с 4 до 6 карточек (сетка 3×2, на мобильном — 1 колонка):
  Лицензия, Аккредитованный центр Hippo, Мини-группы до 7, Команда профессионалов,
  Качественная обратная связь, Мобильное приложение. Контент взят с оригинала.
- Кнопки:
  - Мини-группы → «Смотреть фрагмент урока» → модалка с VK-видео
    (video-191733680_456239500, из wall_514 оригинала).
  - Мобильное приложение → «Посмотреть видеообзор» → модалка с VK-видео
    (video-191733680_456239595, из clip оригинала).
  - Команда → «Познакомиться с командой» → плавный скролл к блоку #fxb-team.
  - Обратная связь → «Посмотреть пример» → модалка с примером (видео-отчёт,
    отчёт об уроке, открытые уроки/тестирования).
- Реализована единая лёгкая модалка-лайтбокс (iframe VK-embed + fallback-ссылка
  «Смотреть во ВКонтакте»), закрытие по оверлею/крестику/Esc.
- Сохранено фирстиль-оформление: градиентные верхние полосы карточек, SVG-иконки,
  scroll-reveal через IntersectionObserver.
- Опубликовано; проверено на лайве (desktop + мобильная ширина): 6 карточек,
  скролл к команде, VK-видео в модалке, модалка-пример — всё работает.

Залогинились заново в Tilda после рестарта (`tilda_login.py`).
Порядок блоков /index-new не менялся (остаётся зафиксированным).

---

## Сессия 15 — страницы курсов: цены, видео-примеры, программа, FAQ + чистка меню

Запрос владельца: на /index-new курс-ссылки должны вести на рабочие копии (-new);
«Летняя академия» открывалась (404 у владельца — кэш); добавить на страницы курсов
цены, видео-примеры и доп. информацию по лучшим практикам.

Ссылки/404:
- Проверено: все видимые курс-ссылки /index-new уже вели на -new (шапка, топбар, подвал),
  «Летняя академия» отдаёт 200 — у владельца был кэш браузера.
- Удалены два скрытых легаси-блока меню старого шаблона (T770 rec2421715951,
  T228 rec2421715961) — единственные места со старыми /reading,/grammar,/preparation.
  После публикации на /index-new 0 старых курс-ссылок, навигацию обеспечивает fxb-shapka.

Страницы курсов (reading-new 151292376, grammar-new 151292406, preparation-new 151292476):
билдер `build_course_pages.py` добавляет блоки (маркеры <!--FXB-EXTRA-->):
- «Программа/этапы» — шаги обучения + «кому подойдёт» (контент с оригиналов).
- «Цена курса» — премиальная карточка:
  Чтение 12 400 ₽ (12 занятий по 60 мин), Грамматика 16 000 ₽ (12×90 мин),
  Подготовка к школе 5 600 ₽/мес (2 раза в неделю по 60 мин); список включённого + CTA.
- Видео-пример «Посмотреть фрагмент урока» (VK video-191733680_456239500) в модалке.
- FAQ-аккордеон (<details>) — вопросы и ответы с оригинальных страниц.

Летняя академия (151229566): факты исправлены под реальные данные
(10 рабочих дней, 10:00–18:00), добавлены блок «Цена смены» 46 000 ₽, видео и FAQ.

Все 4 страницы опубликованы и проверены на лайве. Источники контента — оригиналы
/reading, /grammar, /preparation и страница летней академии. Оригиналы не тронуты.
Порядок блоков /index-new не менялся (остаётся зафиксированным).

## Сессия 16 — HANDOFF (передача другому агенту)

**Ветка:** `devin/1782653000-course-content` (запушена). Последний коммит: тест + правка ссылок шапки/подвала.

### Что сделано в этой сессии
1. Полный тест страниц курсов на лайве — отчёт `prototype/test-report-course-pages.md`, комментарий в PR #13.
   Итог: 8 PASS, 2 FAIL.
2. **FAIL #2 (ссылки шапки) — ИСПРАВЛЕНО в коде, НО ЕЩЁ НЕ ОПУБЛИКОВАНО:**
   - `prototype/tilda_shapka.html`: меню «Наши курсы» (стр. 45-47) и топбар «Летние программы» (стр. 98-99) → /reading-new, /grammar-new, /preparation-new.
   - `prototype/tilda_footer.html`: «Навигация» (стр. 50-52) → -new.

### Что ОСТАЛОСЬ доделать (по запросу владельца «исправь»)
- **FAIL #1 (кнопки заявки не открывают форму)** — НЕ начато.
  Факт: на ВСЁМ сайте форма заявки `onsubmit="return false;"` — бэкенда нет (см. tilda_cta_enrollment.html, main_combined_v7.html). Кнопки `href="#popup:diagnostika"` (в page_*.html стр. 32 и 37) ведут в никуда — попап-блока нет.
  План: добавить рабочий попап-форму на 4 страницы курсов (идемпотентно, маркеры FXB-EXTRA), на submit — маршрутизировать лид в WhatsApp (wa.me/79939232309 с префилл-текстом) + кнопки Max/звонок; показать «спасибо». Заменить `href="#popup:diagnostika"` на `data-fxb-zayavka`.
  Файлы: page_reading.html, page_grammar.html, page_preparation.html, page_letnyaya_akademiya.html (+ build_course_pages.py для воспроизводимости).

### ПОСЛЕ правок — ОБЯЗАТЕЛЬНО опубликовать в Tilda (иначе на лайве не изменится):
1. Залить обновлённую шапку/подвал/контент:
   - `python3 prototype/tilda_upload_subpages.py` (8 доп. страниц: шапка+подвал)
   - `python3 prototype/tilda_upload_copies.py` (reading-new 151292376 / grammar-new 151292406 / preparation-new 151292476)
   - letnyaya-akademiya pageid 151229566 входит в upload_subpages.
   Требуется авторизованный Chrome с CDP :29229 (Tilda-логин уже сохранён, секрет TILDA_PASSWORD в орг).
2. Опубликовать: кабинет проекта 2053071 → «Опубликовать все» (API публикации в Tilda ненадёжен; см. tilda_publish_pages.py как опцию).
3. Проверить на лайве (Ctrl+F5): меню «Наши курсы» в шапке подстраниц → -new; кнопка заявки открывает форму.

### Важные ограничения от владельца
- Оригиналы /reading, /grammar, /preparation НЕ трогать (работаем на -new).
- Порядок блоков на /index-new НЕ менять.

### Ключевые значения
- projectid 2053071; /index-new pageid 151210576
- reading-new 151292376 · grammar-new 151292406 · preparation-new 151292476 · letnyaya-akademiya 151229566
- VK видео урока: oid -191733680, id 456239500
- WhatsApp школы: wa.me/79939232309 ; Max bot: https://max.ru/id611904726658_bot

---

### Сессия 17 (Devin)

**Дата:** 28 июня 2026 · ветка `devin/1782653000-course-content`

#### 1. Рабочий попап-форма заявки на 4 страницах курсов — СДЕЛАНО (в коде)
- `build_course_pages.py`: `price_card()` и CTA теперь рендерят `data-fxb-zayavka` вместо `href="#popup:diagnostika"`.
- Добавлены `zayavka_modal()`, `ZAYAVKA_CSS`, `ZAYAVKA_JS`, `add_zayavka()` (идемпотентно: пропуск, если модалка уже есть).
- Поведение: клик по кнопке → модалка с полями Имя+Телефон (валидация, авто-фокус) → submit формирует текст `Здравствуйте! Хочу записаться на «{КУРС}». Меня зовут {ИМЯ}, телефон {ТЕЛ}.` и открывает `wa.me/79939232309?text=...` → экран «Заявка сформирована!» с альтернативами (WhatsApp/Max/звонок).
- Бэкенда нет — лид уходит в WhatsApp (как и задумано). Заменены все `#popup:diagnostika` (0 осталось), по 5 кнопок + 1 модалка на странице.
- Локально протестировано (HTTP server + Chrome): открытие, валидация, WhatsApp-редирект с подстановкой курса, экран «спасибо».

#### 2. Новые страницы «Новости» и «Вакансии» в едином стиле — СДЕЛАНО (в коде)
- `page_novosti.html` (#fxb-page): hero + 3 карточки последних записей (ссылки на живые посты /news/...) + CTA в группу ВК. Лента новостей динамическая в Tilda — карточки это «слепок» актуальных постов.
- `page_vakansii.html` (#fxb-page): hero + 6 «почему мы» (SVG-иконки, без эмодзи) + 2 вакансии (Преподаватель, Администратор) + 4 шага найма + CTA (Telegram/звонок). Отклик маршрутизируется в Telegram `t.me/m/Zl_g5u6BMWZi` + тел.
- `test_wrapper.html`: предпросмотр любого блока с корректным UTF-8 — `?block=page_novosti.html` (можно перечислять через запятую).

#### 3. По «404 на Летняя академия/Контакты» — НЕ воспроизвелось
- Проверка `curl`: `/letnyaya-akademiya` 200 (fxb-shapka/page/footer), `/kontakty` 200, все пункты меню 200. Реальные 404 только `/novosti` и `/vakansii` (их и закрываем новыми страницами).
- Вероятно был кэш/предпросмотр. Меню шапки/подвала уже ведёт на валидные алиасы.

#### ОСТАЛОСЬ (Tilda-публикация, нужен авторизованный Chrome CDP :29229)
1. `python3 prototype/tilda_upload_subpages.py` — обновит 8 подстраниц (в т.ч. letnyaya-akademiya с попапом) + шапку/подвал с `-new` ссылками.
2. `python3 prototype/tilda_upload_copies.py` — reading-new / grammar-new / preparation-new (попап + шапка/подвал).
3. Новости/Вакансии: контент `page_novosti.html`/`page_vakansii.html` залить на страницы. ВНИМАНИЕ: `/news` использует динамический Feed-блок Tilda (от него зависят посты `/news/<slug>`) — удалять его рискованно. Решение уточнить с владельцем: либо конвертировать `/vacant` (статичная, безопасно) и создать отдельную `/novosti`, либо принять статичный слепок.
4. Кабинет 2053071 → «Опубликовать все» → проверить на лайве (Ctrl+F5).

---

### Сессия 20 (Devin) — формы на всех страницах + SEO/аналитика

**Ветки:** `devin/1783116748-zayavka-modal-fix` (формы, PR #66 merged) → `devin/1783120271-seo-setup` (SEO)

#### 1. Формы «Оставить заявку»/«Записаться» — ИСПРАВЛЕНО и задеплоено (PR #66, merged)
Проблема (от владельца): кнопки заявки не работали на страницах направлений (дошкольники/младшие/подростки/онлайн/поддерживающие/стандартные) и курсов; на главной кнопка просто скроллила вверх; нужно, чтобы в лид попадали предмет + название окна/раздела.

Причины и фиксы:
- **BUG A:** на 10 генерируемых страницах CSS/JS модалки вставлялись ВНЕ тегов `<style>`/`<script>` → модалка была инертна. Плюс не хватало overlay-CSS (`#fxb-page .fxb-modal{position:fixed;inset:0;z-index:99999;...}`).
- **BUG B (регресс при рефакторинге):** жадный regex в `strip_zayavka_artifacts()` вырезал основной CSS/JS дизайна → 10 страниц уехали «раздетыми». `build_subpages.render_page()` перестал добавлять основной `CSS`/`JS`.
- Решение: `build_course_pages.py` — `zayavka_unit()` (самодостаточный блок: модалка + `<style>` + `<script>` + forms.js), `strip_zayavka_artifacts()` переписан на 4 НЕпересекающих regex, инжект внутрь `#fxb-page` (перед его `</div>`). `build_subpages.render_page()` снова добавляет `zayavka_unit()` + основной `CSS`/`JS`.
- Главная: восстановлены 6 карточек цен + метка «ПОПУЛЯРНОЕ»; каждая кнопка открывает модалку с предметом/разделом (`data-fxb-subject`/`data-fxb-window` → скрытые поля Предмет/Раздел/Страница).
- Инвариант на всех 10 live-страницах: `.fxb-card`>0, `IntersectionObserver` есть, ровно 1 `id="fxb-zayavka-modal"`, ровно 1 forms.js, есть `#fxb-page .fxb-modal{position:fixed`, нет «осиротевших» CSS/JS.
- Протестировано вживую (запись): /doshkolniki, /reading (hero+финал), главная (карточка цены) → модалка-оверлей с верным предметом.
- Мелочь (не блокер): на главной кнопка «Отправить заявку» в модалке без жёлтой заливки (косметика).

#### 2. SEO — Яндекс: ГОТОВО
- **Метрика:** на сайте стоял ЧУЖОЙ счётчик №76154395 (не в аккаунте владельца). По решению владельца заменён в Tilda (Настройки → Аналитика) на **№109945462 «Dymova-english»** (аккаунт kidsfoxclub). Вебвизор + карта кликов + авто-цели ВКЛ. Live подтверждён: `mainMetrikaId='109945462'`, старый убран. (GTM-NTM62R9 оставлен.)
- **Вебмастер:** сайт уже подтверждён, ошибок нет. Sitemap: sitemap.xml (21), sitemap-feeds.xml, sitemap-store.xml — статус «ок». Флаг: 6 дублей title / 5 дублей description (см. п.4).

#### 3. SEO — Google: ГОТОВО (аккаунт dymovateacher@gmail.com / «Вероника Дымова»)
- **Search Console:** подключён через OAuth-интеграцию Tilda (SEO → Google Search Console → Подключить) → «Домен подтверждён», sitemap.xml и sitemap-feeds.xml отправлены автоматически.
- **GA4:** создан аккаунт «Фоксинбург» + ресурс «dymova-english.ru» (Москва, RUB), веб-поток (stream 15197140431). **Measurement ID = G-9XMYR6MJGL**. Внесён в Tilda (Настройки → Аналитика → Google Analytics), сайт переопубликован (24/24).

#### 4. SEO — ОСТАЛОСЬ (ветка seo-setup, без участия владельца)
- Дубли: legacy-страницы `/index-old,/grammar-old,/preparation-old,/reading-old,/vacant-old` и `/news` (дубль `/novosti`) отдают 200 → источник 6 дублей title / 5 description. План: в Tilda «Редиректы (301)» (работает только с НЕсуществующих страниц → сперва снять с публикации old-страницы), редиректы: index-old→/, grammar-old→/grammar, preparation-old→/preparation, reading-old→/reading, vacant-old→/vakansii, news→/novosti. `/lmsfoxinburg` → noindex.
- Пустые description: `/novosti`, `/vakansii` — заполнить.
- Метатеги money-страниц (главная, 6 направлений, 4 курса) — по `SEO_meta_recommendations.md` (гео «Долгопрудный» + ключи).
- Schema.org (LocalBusiness/Course/FAQ/BreadcrumbList) + хлебные крошки; внутренняя перелинковка.

#### Ключевые значения (Сессия 20)
- Метрика: **109945462** (заменил чужой 76154395) · GTM: GTM-NTM62R9
- GA4 Measurement ID: **G-9XMYR6MJGL** · GA stream: 15197140431 · GA аккаунт «Фоксинбург»
- Google: dymovateacher@gmail.com · Яндекс: kidsfoxclub@yandex.ru
- PR #66 (формы) merged. Файлы SEO-аудита: `SEO_semantic_core.md`, `SEO_meta_recommendations.md`

---

### Сессия 21 (Devin) — on-page SEO задеплоено + полная настройка Яндекс.Вебмастера

**Ветка:** `devin/1783120271-seo-setup`. Вся правка контента — в кабинете Tilda (2053071), в репозитории — исходники Schema.org + документация.

#### 1. Метатеги title/description — ГОТОВО (14 money-страниц)
По `SEO_meta_recommendations.md` проставлены уникальные title/description с гео «Долгопрудный» и ключами на всех 14 страницах (главная, 6 направлений, 4 курса, /kontakty, /novosti, /vakansii). Заполнены ранее пустые description на `/novosti` и `/vakansii`. Исправлен баг прошлого прогона (у `/vakansii` описание начиналось с «акансии»). Подтверждено на лайве.

#### 2. noindex техстраницы — ГОТОВО
`/lmsfoxinburg` (LMS-платформа) → `<meta robots noindex>` через настройку страницы Tilda (`nosearch`). Подтверждено на лайве.

#### 3. Дубли и 301-редиректы — ГОТОВО
- Удалены 5 устаревших страниц-дублей (`index-old, reading-old, grammar-old, preparation-old, vacant-old`). Нюанс Tilda: 301 срабатывает только с УДАЛЁННЫХ страниц (не со снятых с публикации). После удаления 301 заработали: `index-old→/, reading-old→/reading, grammar-old→/grammar, preparation-old→/preparation, vacant-old→/vakansii`. Проверено на лайве (301).
- Sitemap очищен: `sitemap.xml` = 14 money-страниц + `/news`. `sitemap-store.xml`, `sitemap-feeds.xml` — «ок».

#### 4. `/news` vs `/novosti` — консолидация (решение владельца: основная — /novosti)
`/news` — динамический Feed Tilda с 3 постами (`/news/school`, `/news/sydhatdsd1-...`, `/news/u3yp7rk1i1-2-smena`), которые нужно сохранить. Прямой 301/удаление невозможны (сломают посты). Настройка noindex на самой Feed-странице КАСКАДИРУЕТ на посты (проверено: посты получали лишний `noindex`) — откатил.
- **Решение:** path-exact `noindex,follow` для РОВНО `/news` через JS-инжектор в общем `<head>` (проверка `location.pathname === '/news'`). Посты `/news/<slug>` не затрагиваются (у них свой `index,follow`). Проверено рендером: `/news`→`noindex`, `/news/school`→`index,follow`.

#### 5. Schema.org — ГОТОВО (общий `<head>` сайта)
- Статически (@graph): `EducationalOrganization`+`LocalBusiness` (2 филиала: Лихачевский 76к1 — +7 993 923-23-09, 141701; Ракетостроителей 9к3 — +7 916 732-31-69, 141707), `WebSite`.
- По-страничный JS-инжектор (pathname→JSON-LD): `FAQPage` (главная), `Course`+`BreadcrumbList` (reading/grammar/preparation/letnyaya-akademiya), `BreadcrumbList` (остальные разделы). Проверено рендером на `/`, `/reading`, `/kontakty`, `/novosti`.
- Исходники: `prototype/seo_schema/` (org_localbusiness.html, course_*.html, breadcrumb_*.html, faq.html, DEPLOY_MAP.md).

#### 6. Яндекс.Вебмастер — ПОЛНАЯ НАСТРОЙКА (аккаунт kidsfoxclub@yandex.ru)
- **Диагностика:** «Ошибок нет». 2 рекомендации — некритичные (уведомления/сбор запросов).
- **Переобход страниц:** отправлен 21 URL (14 money + 7 устаревших `/vacant, /news, *-old` для ускоренного выпадения из индекса). Дневной лимит 560.
- **Региональность:** Долгопрудный (из Яндекс.Бизнес, карточка «Фокси Фокс» ★4.2 привязана) + Москва и МО (Вебмастер) — оставлено (широкий + точный охват).
- **JS-рендеринг:** переключён на «Рекомендую рендерить» — критично, т.к. по-страничная Schema и `/news`-noindex инжектятся через JS.
- **Обход по счётчикам:** счётчик Метрики **109945462** привязан к сайту, обход **включён** (робот быстрее видит изменения).
- **Sitemap:** все 3 карты зарегистрированы, статус «ок».
- **Зеркала (переезд):** HTTPS уже главное зеркало — без изменений.
- **robots.txt:** валиден, содержит 3 директивы Sitemap.
- **Быстрые ссылки:** доступный кандидат «Занятия» включён к показу (остальные сгенерируются после переобхода новой структуры/схемы).
- **Дубли title/description (6/5):** остаточные от удалённых страниц; выпадут после переобхода (Яндекс обновляет в течение недели).

#### 7. Аудит H1 — НАЙДЕНЫ 2 замечания (правка структуры дизайна, к владельцу)
- Главная — **2 H1** («Английский не для школы,» + «Курсы английского языка в Долгопрудном»).
- `/kontakty` — **0 H1** (верхний заголовок — H2).
- Остальные 12 страниц — по 1 H1 (ок). Правка тегов заголовков в Zero-блоках Tilda рискует вёрсткой → согласовать с владельцем.

#### 8. Google Search Console — ТРЕБУЕТСЯ ДОСТУП
Ресурс `sc-domain:dymova-english.ru` подтверждён НЕ под `dymovateacher@gmail.com` (у него нет прав), а вероятно под `vkrivobokova4@gmail.com` (OAuth-аккаунт Tilda). Чтобы сделать `dymovateacher` владельцем — нужен вход под `vkrivobokova4` (пароль) либо доступ к DNS домена. Ожидает данных от владельца.

#### Ключевые значения (Сессия 21)
- Метрика **109945462** (привязана к Вебмастеру, обход вкл) · GA4 **G-9XMYR6MJGL** · GTM-NTM62R9
- Яндекс: kidsfoxclub@yandex.ru · Google: dymovateacher@gmail.com (нужен доступ vkrivobokova4 для GSC)
- Money-страницы (14): /, /doshkolniki, /mladshie-shkolniki, /podrostki, /reading, /grammar, /preparation, /online-zanyatiya, /podderzhivayushchie-online, /standartnye-offline, /letnyaya-akademiya, /kontakty, /novosti, /vakansii

---

### Сессия 22 (агент — Devin, «не выдумываем»: перевод на администратора + журнал пробелов) — PR #TBD

**Дата:** 2026-07-08
**PR:** #TBD — Foxi: уверенные ответы без выдумывания, мягкий перевод на администратора, журнал вопросов без ответа (devin/1783540723-foxi-no-hallucination → main)
**Запрос владельца:** «Сделай Фокси идеальным: не выдумывал информацию, отвечал уверенно; если не знает точного ответа — мягко сообщить и перевести на администратора; записывать в память вопросы без ответа и предоставить в отчёте».

**Что сделано:**
- `app/sales.py` — SYSTEM_PROMPT: требование отвечать уверенно (без «наверное/возможно») когда факт есть в контексте; если ответа в контексте НЕТ — начинать ответ с токена `[UNKNOWN]` и мягко отсылать к администратору, без выдуманных деталей.
- `app/ai_core.py` — новый `_refer_to_admin()`: логирует вопрос в журнал пробелов (`insights.log_gap`), уведомляет администраторов (`hand_off`) и отвечает клиенту мягкой фразой («точной информации нет, придумывать не хочу, передал администратору» + телефоны), при этом НЕ блокирует диалог (stage возвращается в discovery). Срабатывает: (а) LLM вернул `[UNKNOWN]`/фразы неуверенности; (б) LLM недоступен и в базе знаний ничего нет; (в) фактический вопрос (цена/контакты/курсы/о школе) без совпадений в KB. Слабое совпадение KB (score < 0.34) логируется как `weak_kb_match`, но бот всё равно отвечает.
- `app/knowledge/kb.py` — `search_scored()` (поиск со скором), `search()` теперь обёртка.
- `app/config.py` — добавлены отсутствовавшие настройки `INSIGHTS_FILE` (по умолчанию `./data/insights.jsonl`), `DIGEST_ENABLED/HOUR/MINUTE/TZ_OFFSET/DAYS`, `NUDGE_ENABLED/HOUR/MINUTE` — раньше `insights.py` и `scheduler.py` падали с AttributeError (стабильность!).
- `app/main.py` — планировщик (`scheduler.start()`) теперь запускается на startup: ежедневный дайджест диалогов + дайджест «слабых вопросов» администраторам в MAX (21:00 МСК) и тёплые напоминания (11:00 МСК). Новые эндпоинты: `GET /admin/insights?days=&top=` (сводка вопросов без ответа) и `POST /admin/digest/send` (отправить отчёт сейчас), оба под `X-Admin-Token`.
- `.env.example` — задокументированы новые переменные.
- Тесты: новый `tests/test_no_hallucination.py` (7 тестов: маркер `[UNKNOWN]`, фразы неуверенности, no_answer без LLM, weak_kb_match, наличие настроек, требования в промте); обновлён 1 ассерт в `test_quality_fixes.py` под новую формулировку ответа (поведение стало строже: раньше «не нашёл данных, могу передать», теперь реально передаёт и логирует).

**Как проверено:**
- тесты: `cd bot && pytest -q` → 159 passed.

**Решения и нюансы:**
- Перевод на администратора при незнании НЕ переводит диалог в STAGE_HANDOFF навсегда: админы получают уведомление с контекстом, клиенту даётся мягкий ответ, но бот продолжает помогать по другим темам (иначе один неизвестный вопрос «замораживал» бы диалог).
- Журнал пробелов — JSONL (`INSIGHTS_FILE`), user_id хэшируется, персональные данные не пишутся. Отчёт по нему уходит админам ежедневно вместе с дайджестом диалогов и доступен по `/admin/insights`.
- `_is_uncertain_reply()` ловит и токен `[UNKNOWN]`, и типовые фразы неуверенности — защита от моделей, игнорирующих инструкцию про токен.

**Деплой:** прод НЕ переразвёрнут (нет SSH в этой сессии). После мержа: на ВМ `git pull && docker compose up -d --build`; убедиться, что в `.env` прода есть `ADMIN_MAX_IDS` (иначе дайджест и уведомления некому слать) и по желанию переопределить `INSIGHTS_FILE`/`DIGEST_*`.
**Осталось / следующий шаг:** переразвернуть прод; понаблюдать неделю за `/admin/insights` и дозаполнить базу знаний по топу вопросов; фичи №10 (сбор отзывов) и №11 (голосовые) из плана владельца.

---

### Сессия 23 (агент — Devin, e2e-тестирование Фокси + фикс пробелов в секретах) — PR #TBD

**Дата:** 2026-07-08
**PR:** #TBD — fix(bot): секреты с пробелами больше не ломают LLM (devin/*-strip-secrets → main)
**Запрос владельца:** «Протестируй бота по темам школы/цен/курсов/материалов, путь холодного и текущего клиента, без галлюцинаций» + «исправь сам» проблему с API-ключом.

**Что сделано:**
- Проведено e2e-тестирование через виджет foxi.js (`bot/run_local_test.py`, локально на :8000): 11 проверок пройдены, галлюцинаций нет. Отчёт: комментарий к PR #116.
- Найден баг: `LLM_API_KEY` с завершающим пробелом → `Illegal header value` → все LLM-вызовы падали, бот всё переводил на администратора.
- `app/config.py` — `field_validator` `_strip_secret`: обрезает пробелы/переводы строк у `LLM_API_KEY`, `MAX_BOT_TOKEN`, `BIGBEN_API_KEY`, `ADMIN_TOKEN`, `TELEGRAM_BOT_TOKEN`, `MAX_WEBHOOK_SECRET`, `TELEGRAM_WEBHOOK_SECRET`.
- Новый тест `tests/test_config_strip.py`.

**Как проверено:** `cd bot && pytest -q` → 160 passed.

**Решения и нюансы:**
- Бесплатная модель `meta-llama/llama-3.3-70b-instruct:free` часто в rate-limit (429); тестирование велось на `openai/gpt-4o-mini`. В проде рекомендована платная модель (у владельца уже подключена).
- Ключ владельца в чате совпадает с уже сохранённым секретом; проблема была именно в пробеле — теперь код устойчив к этому.

**Деплой:** после мержа — на ВМ `git pull && docker compose up -d --build`.
**Осталось / следующий шаг:** наблюдать `/admin/insights`, дозаполнять KB по топу вопросов.

---

### Сессия 24 (агент — Devin, деплой прода + восстановление Telegram long-polling) — PR #TBD

**Дата:** 2026-07-08
**PR:** #TBD — fix(bot): восстановлен запуск Telegram long-polling на startup (devin/1783545164-telegram-polling-restore → main)
**Запрос владельца:** редеплой прода (#116/#117).

**Что сделано:**
- Прод переразвёрнут из `main` (fb34618): SSH `yc-user@89.169.132.104`, `cd ~/Dymova-english && git pull && cd bot && docker compose -f docker-compose.yml build && up -d`. ВНИМАНИЕ: `docker compose -f docker-compose.prod.yml` поднимает СТАРЫЙ контейнер `foxinburg-maxbot` — им НЕ пользоваться (остановлен обратно). Рабочий контейнер — `bot-bot-1` из `docker-compose.yml`; после пересоздания подключить к сети `foxinburg_foxinburg` (nginx `foxinburg-frontend` → `bot-bot-1:8000`).
- Проверено: `/health` ok (llm_providers:2, kb_documents:45), `/api/chat` отвечает реальными ценами, `/admin/insights` 200 с токеном / 401 без.
- НАЙДЕНА РЕГРЕССИЯ: при консолидации `main.py` (после Сессии 16) потерялся запуск `_telegram_poll_loop` на startup и настройка `TELEGRAM_POLLING` — Telegram-бот молчал бы. Исправлено: `TELEGRAM_POLLING: bool = True` в config, запуск поллинга в `_start_scheduler` при настроенном клиенте, устойчивость цикла к ошибкам (лог + sleep 3).

**Как проверено:** `pytest -q` → 160 passed; после деплоя фикса — лог `telegram: запуск long-polling` в контейнере.

**Деплой:** прод обновлён этой сессией (включая фикс поллинга — см. ниже).
**Осталось / следующий шаг:** наблюдать /admin/insights неделю, дозаполнить KB; фичи №10/№11.

---

### Сессия 24b (агент — Devin, горячий фикс после жалобы владельца) — PR #TBD

**Дата:** 2026-07-08
**Жалоба владельца:** «Бот в максе молчит! а в ТГ ответил не то что надо!»

**Диагноз по логам прода:**
1. MAX молчал: ответ отправлялся с кнопками из `_button_rows` в «виджетном» формате `{"title","url"}` — MAX API отвечал 400 `Can't deserialize body`, сообщение не доставлялось (у Telegram есть нормализация в `_normalize_buttons`, у MAX — нет).
2. «Ответил не то что надо»: фактические интенты (ABOUT/PRICE/COURSES/CONTACTS) в `_route` шли мимо LLM сразу в `_grounded_fact_reply` — сырой дамп топ-4 документов KB, нерелевантный вопросу («Кто у вас педагоги?» → факты про пробное занятие).

**Фиксы:**
- `main.py`: в MAX-ветке `message_created` кнопки строятся через `_link_button_rows` (валидный формат MAX `{"type":"link","text","url"}`); `_button_rows` удалён.
- `ai_core.py`: фактические интенты при включённом LLM идут через `_consult` (LLM + KB-контекст + анти-галлюцинационная проверка `_is_uncertain_reply` → перевод на администратора); `_grounded_fact_reply` остаётся фолбэком без LLM.

**Как проверено:** pytest 160 passed; после деплоя — живые вопросы в MAX/ТГ, ответы приходят и релевантны.

---

### Сессия 24c (агент — Devin) — педагоги в KB, ссылка на чат клиента, живая синхронизация с сайтом

**Дата:** 2026-07-08
**Запрос владельца:** 1) «бот должен писать Фамилию Имя, какой язык преподаёт и ссылки на видеовизитку и фрагмент урока у каждого педагога»; 2) «при создании заявки из макса администратору приходит ссылка на чат, но она не работает»; 3) «бот должен уметь моментально просматривать актуальную информацию на сайте, соцсетях и каналах».

**Диагноз:**
1. В `bot/app/knowledge/data.yaml` вообще не было данных о команде — на вопросы о педагогах бот честно переводил на администратора (анти-галлюцинация работала правильно, данных не было).
2. Ссылка `https://max.ru/chat/{user_id}` в `admin_router._client_contact_block` — невалидный формат MAX (404). Валидные ссылки MAX — `https://max.ru/{username}`, а username приходит в webhook-поле `sender`.
3. KB обновлялась только из data.yaml — актуальный контент сайта не попадал в ответы.

**Фиксы:**
- `data.yaml`: секция `team` — 10 человек из блока «Команда» сайта (prototype/tilda_team.html): Саляхова Алина (англ+нем), Дмитроченко Юлия (англ), Птицын Владислав (англ), Анохин Роман (англ, видео нет), Шевченко Дарья (кит), методист Коваленко, администраторы Головизнина/Джанузакова, руководители Дымовы. Видеовизитки/фрагменты уроков — raw.githubusercontent ссылки из prototype/team-media (те же, что на сайте).
- `kb.py`: индексация `team` (документ на человека + сводный документ со всеми педагогами), бонус скоринга для категории team, `live_documents` + `set_live_documents()` для живых документов с сайта.
- `admin_router.py`: MAX-ссылка строится из `conv.max_username` (`https://max.ru/{username}`), без username — только `ID:`; имя клиента из `conv.client_name`, если ФИО не собрано.
- `memory.py`: поля `client_name`, `max_username` в Conversation (+ `_conv_from_dict`).
- `main.py`: `_remember_sender()` — сохраняем name/username отправителя из каждого MAX-сообщения.
- НОВОЕ: `knowledge/site_sync.py` — периодическая (SITE_SYNC_INTERVAL_MIN=60) выгрузка страниц сайта (SITE_SYNC_URLS, по умолчанию главная dymova-english.ru), разбивка HTML на текстовые чанки (60–700 символов, максимум 80/страницу, дедупликация), загрузка в KB как категория `site`. При ошибке сети прежние живые документы сохраняются. Цикл — в `scheduler.start()` (SITE_SYNC_ENABLED=true). В тестах выключено через conftest.
- По соцсетям (VK/MAX/TG-каналы) прямого API-доступа у бота нет — владельцу предложено начать с сайта; соцсети потребуют отдельных токенов/парсеров.

**Как проверено:** pytest 166 passed (новый tests/test_team_and_site_sync.py: поиск педагогов, ссылки на видео, сводный документ, MAX-ссылка по username, extract_chunks, поиск по live-документам); живой sync_once() с реального сайта → 80 документов; тесты test_admin_contact.py обновлены под новый формат ссылки (старый был предметом жалобы владельца).

**Деплой:** после мержа — `ssh yc-user@89.169.132.104 'cd ~/Dymova-english && git pull && cd bot && docker compose build && docker compose up -d'` (контейнер `bot-bot-1`, при пересоздании подключить к сети `foxinburg_foxinburg`).
**Осталось / следующий шаг:** проверить в MAX/ТГ ответы про педагогов (ФИО+язык+ссылки); для username-ссылки нужен username у клиента в MAX (без него в заявке будет только ID); обсудить доступ к соцсетям (VK API-токен, TG-канал).

---

### Сессия 24d (агент — Devin) — структурированный ответ о педагогах со ссылками

**Дата:** 2026-07-08
**Запрос владельца:** «бот перечислил педагогов английского, но нужно в формате таблички или по пунктам сразу со ссылками на видеовизитки и фрагменты уроков каждого педагога!»

**Диагноз (переписка в MAX, user 5897639):** LLM на «У вас есть педагоги Английского?» перечислял имена без ссылок, а «А есть видео с Алиной?» уходило к администратору — «Алиной» не матчилось со стемом «Алина» (в стеммере kb._stem не было суффикса «ой»).

**Фиксы:**
- `ai_core.py`: новый детерминированный `team_reply(kb, text)` — на вопросы о педагогах (педагог/учител/препода/визитк/фрагмент урока/кто ведёт или имя педагога в тексте) бот отвечает по пунктам: 👩‍🏫 ФИО — язык, ▶️ Видеовизитка: URL, 🎬 Фрагмент урока: URL. Фильтрация по языку (англ/нем/кит); упоминание имени в любой форме («с Алиной») → карточка конкретного педагога. Вызов в `_route` до `_FACTUAL_INTENTS`, кроме intent WANT_SIGNUP. LLM для этих вопросов не используется — формат стабилен, ссылки не теряются.
- `kb.py`: суффиксы «ой», «ёй», «ами» в `_stem` (Алиной→алин).

**Как проверено:** pytest 169 passed (3 новых теста в test_team_and_site_sync.py: список педагогов языка со ссылками, карточка по имени в косвенном падеже, None для нерелевантных вопросов); test_quality_fixes FakeKB без .raw учтён (getattr).

**Деплой:** после мержа — стандартный редеплой (git pull, docker compose build && up -d, контейнер bot-bot-1, при пересоздании подключить сеть foxinburg_foxinburg).
**Осталось / следующий шаг:** живая проверка в MAX владельцем.

---

### Сессия 25 (Kimi Code CLI) — канонические URL для курсовых страниц

**Дата:** 2026-07-09
**Запрос владельца:** «Нужно переименовать адреса сайтов: с NEW на /, а старые поставить OLD. Страницы, что сейчас открываются, правильные, но нужен адрес просто /reading и т.д.»

**Контекст:**
- Новые страницы с единым дизайном были выложены на URL `/reading-new`, `/grammar-new`, `/preparation-new`.
- Старые оригиналы занимали канонические URL `/reading`, `/grammar`, `/preparation`, но устарели.
- Владелец потребовал сделать новые страницы доступными по каноническим URL, а старые — по URL с суффиксом `-old`.

**Что сделано:**
- Обновлены прототипы навигации:
  - `prototype/tilda_shapka.html` — ссылки `/reading-new` → `/reading`, `/grammar-new` → `/grammar`, `/preparation-new` → `/preparation`.
  - `prototype/tilda_footer.html` — аналогично.
  - `prototype/tilda_blocks_min/tilda_footer_min.html` — минифицированная версия подвала синхронизирована.
- Обновлены скрипты публикации/синхронизации с Tilda:
  - `prototype/tilda_update_nav.py` — aliases новых страниц изменены на `reading`, `grammar`, `preparation`.
  - `prototype/tilda_upload_copies.py` — aliases и докстринги приведены в соответствие.
  - `prototype/tilda_relink_index.py` — направление замены инвертировано: `/reading-new` → `/reading` и т.д.
  - `prototype/tilda_set_settings.py` — добавлены настройки для всех 6 страниц:
    - новые: `/reading` (151292376), `/grammar` (151292406), `/preparation` (151292476);
    - старые архивы: `/reading-old` (137726126), `/grammar-old` (137739566), `/preparation-old` (130390566).
- Обновлена документация:
  - `wiki/02-arhitektura.md` — отражены канонические URL и архивные `-old` копии.

**Как применено в Tilda:**
1. Через Kimi WebBridge (Chrome уже был авторизован в Tilda) запущен новый скрипт `prototype/tilda_set_settings_webbridge.py`.
2. Установлены канонические alias'ы для новых страниц:
   - `151292376` → `/reading`
   - `151292406` → `/grammar`
   - `151292476` → `/preparation`
3. Опубликованы страницы скриптом `prototype/tilda_publish_pages_webbridge.py`.
4. Обновлены шапка/подвал на всех 22 страницах сайта скриптом `prototype/tilda_update_nav_webbridge.py` и опубликованы.
5. Проверены живые URL:
   - `curl -I https://dymova-english.ru/reading` → 200 ✅
   - `curl -I https://dymova-english.ru/grammar` → 200 ✅
   - `curl -I https://dymova-english.ru/preparation` → 200 ✅
   - `curl -I https://dymova-english.ru/reading-new` → 404 ✅
   - `curl -I https://dymova-english.ru/grammar-new` → 404 ✅
   - `curl -I https://dymova-english.ru/preparation-new` → 404 ✅

**Примечание:** старые оригиналы (`137726126`, `137739566`, `130390566`) оказались в корзине Tilda, поэтому канонические URL были свободны и не потребовалось восстанавливать их как `-old`. Если в будущем понадобится архивная копия, её можно восстановить из корзины и задать alias с суффиксом `-old`.

**Деплой:** изменения касаются только репозитория и Tilda-настроек; бэкенд/бот не затронуты.
**Осталось / следующий шаг:** продолжить работу над скоростью/SEO (WebP, lazy-load, CLS) — следующий приоритет владельца.

---

### Сессия 25b (Kimi Code CLI) — WebP для брендовых изображений в навигации

**Дата:** 2026-07-10
**Запрос владельца:** «сначала 3 и 2» — после фикса URL перейти к скорости/SEO.

**Что сделано:**
- Сконвертированы ключевые брендовые PNG в WebP:
  - `fox-head-yellow.png` 138.5K → `fox-head-yellow.webp` 50.2K (−64%)
  - `fox-head-purple.png` 138.3K → `fox-head-purple.webp` 47.2K (−66%)
  - `decor-swirl-yellow-1.png` 64K → `.webp` 8.8K (−86%)
  - `decor-swirl-yellow-2.png` 74.5K → `.webp` 26.1K (−65%)
  - `decor-zigzag-yellow.png` 59.7K → `.webp` 17.2K (−71%)
  - `decor-blob-yellow.png` 38K → `.webp` 10.3K (−73%)
- Обновлены ссылки в прототипах: `tilda_header_unified.html`, `tilda_shapka.html`, `tilda_footer.html`, `tilda_cta_enrollment.html`, `tilda_slogan.html`, `tilda_pricing_enrollment.html`, `tilda_team.html`, `main_combined_v7.html`.
- Пересобраны minified-блоки (`make all`) и выложены в Tilda через `tilda_update_nav_webbridge.py` на все 22 страницы.

**Как проверено:**
- `curl` по `/`, `/reading`, `/grammar`, `/preparation`, `/doshkolniki`, `/oge-anglijskij`:
  - 0 ссылок с суффиксом `-new`;
  - 5–7 WebP-изображений на странице (в шапке/подвале).

**Деплой:** изменения закоммичены (`71aa941`) и запушены в `main`.
**Осталось / следующий шаг:**
- Развернуть WebP и в контент-блоках главной страницы (team, pricing, CTA, directions и др.), чтобы снизить LCP.
- Добавить `loading="lazy"`/`fetchpriority="low"` декоративным изображениям.
- Перейти к крупным пунктам аудита: отложенная загрузка карт/форм, font-display swap, фиксация CLS.
